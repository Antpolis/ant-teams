# Builder Memory

## Active Lessons

### 2026-08-03 - SPEC-002 / #24 (PR #28, loop 1)

- Context: SPEC-002-T3 — comprehensive managed-sync test suite (TEST-1 unit, TEST-2 integration 14 scenarios, TEST-3 e2e 4 scenarios, TEST-4 FR/ERR coverage matrix). Test-only PR: 31 new files under `tests/`, ZERO changes to `scripts/` or prior `tests/` (scope boundary for #23 impl / #25 docs respected). Built on the merged #22 (`sync-managed-skills.sh`) + #23 (`sync-company.sh` integration) at `origin/master` `0b6d0e3`.
- Implementation Lesson (fixture-copy technique for self-rooting scripts): `sync-managed-skills.sh` resolves `REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"` and reads `.opencode/{skills,commands}` from there — there is NO `--source-dir`/`--target-dir` env override (CLI-1.2 lists only `--force`/`--dry-run`). To run the REAL script against CONTROLLED fixtures (corrupt manifest, collisions, `..` filenames, partial-run state), COPY the real script into `<fixturerepo>/scripts/sync-managed-skills.sh` and stage a controlled `<fixturerepo>/.opencode/`. The script then resolves REPO_ROOT to the fixture and runs production code byte-for-byte. Tests override `$HOME` to a `mktemp -d` temp dir (TR-1.3 resolves `~` via `$HOME`) so real `~/.agents/skills` + `~/.config/opencode` are never touched. A symlink to the script is fragile (cd/pwd logical-vs-physical); COPY is unambiguous.
- Implementation Lesson (the script REQUIRES `.opencode/commands` to exist): lines 491–496 `die_usage` if `SOURCE_COMMANDS_DIR` is missing. A fixture with only `.opencode/skills/` exits 1 ("source commands directory not found"). `sync_make_fixture_repo` pre-creates BOTH empty `.opencode/skills/` and `.opencode/commands/` for this reason. The script also requires `sha256sum`/`shasum` AND `node` (node backs manifest JSON I/O via an inline helper; missing node → exit 4).
- Implementation Lesson (idempotency snapshot MUST exclude `.manifest.json`): FR-10.1 explicitly permits the manifest `last_sync` to be REWRITTEN on a no-op re-run (the script always rewrites the manifest atomically as the last write). So a full-tree `find ... -exec sha256sum` snapshot taken before+after run 2 will DIFFER (manifest changed). Exclude `.manifest.json` from the tree snapshot (`find ... -not -name '.manifest.json'`) and assert the manifest-only diff separately via `grep -v '"last_sync"' manifest | sha256sum` (TR-4.3: last_sync is the only field allowed to differ).
- Implementation Lesson (dry-run action lines are per-FILE, not per-ENTRY): `[DRY-RUN] [INSTALL]` (and `[INSTALL]`/`[UPDATE]` in real mode) is emitted once per managed FILE (OBS-1.2), so the real repo's dry-run reports ~82 install lines, NOT 34. The ENTRY count is in the summary line `Total managed entries: N` (MANAGED_ENTRIES_COUNT is per-entry). To assert "34 entries" in dry-run/e2e, grep the summary line, not the action-line count.
- Implementation Lesson (e2e raw-dir count includes unmanaged siblings): `find ~/.agents/skills -mindepth 1 -maxdepth 1 -type d | wc -l` counts ALL dirs including unmanaged ones (AC-2.1 acknowledges unmanaged siblings coexist). The EXACT managed count is the manifest's `managed_entries` length (`sync_manifest_count_entries` via `node -e`); use `assert_ge` for raw dir count when an unmanaged sibling is seeded.
- Implementation Lesson (boundary test: assert escaping PATH absent, not whole entry DIR): the script processes earlier in-bounds files before reaching a bad path. A fixture skill with a `..` filename triggers `assert_target_within_managed`'s `case "$rel" in *..*)` → `die_boundary "path traversal detected in target"` (exit 2, NOT "boundary" in the message — assert substring "traversal"). The skill's `SKILL.md` (no `..`) IS written first; only the escaping subdir/file is never created. Assert `<target>/<skill>/<..subdir>` absent + exit 2, NOT "no managed dir created". A malicious manifest `target_path` outside the subtree is NOT used for writes (script computes target from entry name+subpath, never the manifest's stored target_path — SEC-2.1); assert no out-of-bounds FILE appears, exit stays 0 if the entry is "modified"/preserved.
- Implementation Lesson (ERR-1.2 command-unreadable GAP — implementation divergence): the spec's graceful "unreadable command → [ERROR] + skip + continue + exit 1" is NOT what the script does. Command files are STAGED during discovery via `generate_command_skill` (which runs `tail -n +N "$src"` inside a `{ } > out` block) BEFORE the per-entry readability check. An unreadable command source makes `tail` fail → `set -e` aborts the whole script (exit 1) with a raw "tail: cannot open ... Permission denied" on stderr (NOT the spec's `[ERROR]` format), and NO further entries are processed. ERR-2.1 (unreadable SOURCE SKILL file) DOES work gracefully (per-entry readability check at lines 704–717 catches `[[ ! -r "$_src" ]]` for source skills because their `_src` is the original file, not a staged copy). ERR-1 is still covered via ERR-1.1 (unclosed/malformed frontmatter → `[WARNING]` + default desc ""). Do NOT write a test asserting the spec's ERR-1.2 graceful behavior for commands — it will fail and the fix is out of test-issue scope. *(SUPERSEDED in loop 2 — tech-lead Option B authorized the minimal staging-guard fix; see the loop-2 entry below. Kept for historical accuracy of the loop-1 state.)*
- Implementation Lesson (helper calling-convention gotcha — assert_exec): the e2e `assert_exec PATH` takes ONE arg (path only), while every other `assert_*` takes `(LABEL, actual, ...)`. Calling `assert_exec "label" "$path"` passes the label as the path → `[[ -f "label" ]]` false → spurious FAIL. My `tests/lib/sync_helpers.sh` uses `(LABEL, PATH)` consistently to avoid this; if reusing the e2e lib's `assert_exec`, pass the path alone.
- Implementation Lesson (do NOT source test helpers in the zsh tool shell): the opencode Bash tool runs under ZSH, where `${BASH_SOURCE[0]}` is NOT populated when `source`-ing a file. `dirname "${BASH_SOURCE[0]}"` with empty BASH_SOURCE → `.` → wrong SYNC_REPO_ROOT (drops the `ant-teams/issue-24` suffix). This produces misleading ad-hoc repro failures. Always debug via `bash -c '...'` (matches the target shebang and populates BASH_SOURCE correctly), not by sourcing in the tool shell.
- Files/Modules: `tests/lib/sync_helpers.sh`, `tests/test_sync_{unit,int,e2e}_*.sh` (24 scenario files + 4 extra + 1 coverage matrix), `tests/run_sync_tests.sh`. Only NEW files under `tests/`. No `scripts/` or prior-test edits.
- Verification: `bash tests/run_sync_tests.sh` → 29/29 scenarios, 355 assertions; `for f in tests/test_sync_*.sh; do bash "$f"; done` → all pass (issue's literal cmd); prior SPEC-002 smoke (#22) 70/70 (additive); `tests/run_e2e_tests.sh` 10/10; `tests/test_*.js` 8/8 (466 assertions); `validate-agents-md.sh` 11/11; `bash -n` clean. Real install = 34 managed entries (26 source + 8 command-derived, 0 collisions).
- Avoid: Do NOT modify `scripts/sync-managed-skills.sh` or `scripts/sync-company.sh` (#22/#23 own them; this issue is tests-only). Do NOT commit SPEC-002/ARCH-004 docs (local-only reference; #25 owns the docs + DOCUMENT_INDEX). Do NOT add a `--target-dir`/env override to the managed script to "make tests easier" (CLI-1.2 forbids it; use the fixture-copy + HOME override). Do NOT assert the spec's ERR-1.2 graceful-command path (implementation diverges).
- Process Note: Issue #24 project item ID `PVTI_lADOAGcCyM4Bdw3Lzg046wg` (workflowState field `PVTSSF_lADOAGcCyM4Bdw3LzhYQD90`, ready=`3cd868ce`, in-progress=`430925ac`, in-review=`3cbc15c8`, ready-to-merge=`894e062a`, done=`f46ff79d`); `gh project item-edit --project-id PVT_kwDOAGcCyM4Bdw3L` works (the bundled `gh_project_helper.sh set-status` still falls back to a `PVT_kwDOEXAMPLE` placeholder — use `gh project item-edit` directly). The "Status" field (PVTSSF_lADOAGcCyM4Bdw3LzhYQDlE, todo/in-progress/done only) is legacy; the 9-state "Workflow State" field is canonical — leave Status at In Progress when moving to In Review (no In Review option exists on Status). Main checkout still has uncommitted docs/DOCUMENT_INDEX.md + untracked docs (ARCH-004/SPEC-002 local-only, committed by #25), so worktree created directly via `git worktree add -b feat/spec-002-t3-managed-sync-tests ~/Projects/worktree/ant-teams/issue-24 origin/master`. PR body + handover posted via `gh pr create/comment --body-file` (backtick/`$()` escaping in zsh).
- Related Docs: `docs/spec/SPEC-002-managed-global-skill-sync-and-command-derived-skills.md` (TEST-1..4, FR-1..12, ERR-1..6, DM-1..4, SEC-1..5), `docs/arch/ARCH-004-managed-skill-sync-architecture.md` (Components 1-4, data flow), PR #28, issue #24 activation comment 5158446710.

### 2026-08-03 - SPEC-002 / #24 (PR #28, loop 2 — ERR-1.2 fix)

- Context: Reviewer loop-1 finding (TEST-4.2 overstated ERR-1.2) resolved via [tech-lead Option B decision](https://github.com/Antpolis/ant-teams/issues/24#issuecomment-5163449843): minimal implementation fix, not a spec narrowing. This SUPERSEDES the loop-1 "Do NOT assert ERR-1.2 / fix is out of scope" guidance above.
- Implementation Lesson (the ERR-1.2 fix is a 3-line `test -r` guard in the COMMAND-STAGING loop, NOT the per-entry loop): in `scripts/sync-managed-skills.sh` the staging loop `for cmd_file in "$SOURCE_COMMANDS_DIR"/*.md` calls `generate_command_skill` (which reads the file via `head`/`awk`/`tail`) BEFORE the per-entry readability check (lines ~712). An unreadable command source therefore aborted under `set -e`. The fix adds `if [[ ! -r "$cmd_file" ]]; then err "..."; HAD_SOURCE_ERROR=1; continue; fi` as the FIRST statement inside that staging loop, before `name="$(basename ...)"`. The existing per-entry check (lines ~704–718) is UNTOUCHED and still owns ERR-2.1 for source skills. Exit 1 comes from the existing `HAD_SOURCE_ERROR` gate at the script tail (line ~928, `exit "$EX_USAGE"` where `EX_USAGE=1`).
- Implementation Lesson (empty-glob is safe — `shopt -s nullglob` at line 511): when there are no `.md` command files, nullglob makes the glob expand to nothing and the loop body (including the new guard) never runs. The guard does NOT change empty-repo behavior.
- Implementation Lesson (ERR-1.2 test mirrors ERR-2.1's non-root guard): added a second scenario to `tests/test_sync_unit_source_errors.sh` — two sibling commands (`badcmd.md` chmod 000 + `goodcmd.md`), asserts exit 1 + `[ERROR]` + `badcmd.md` path named + badcmd NOT installed + **goodcmd still installs** (proves skip+continue). Both scenarios share one `sync_begin`/`sync_done`; the EXIT trap initializes `FIX="" HOME_DIR="" FIX2="" HOME_DIR2=""` up front so it is safe under `set -u` on the root-skip early exit. Coverage matrix ERR-1 line now lists `test_sync_unit_source_errors.sh` alongside the ERR-1.1 frontmatter files.
- Files/Modules: `scripts/sync-managed-skills.sh` (staging loop, +9 incl. comment), `tests/test_sync_unit_source_errors.sh` (ERR-1.2 scenario + header), `tests/test_sync_coverage_matrix.sh` (ERR-1 mapping). Diff +56/−14.
- Verification: `bash tests/test_sync_unit_source_errors.sh` → 12/12 (ERR-2.1 + ERR-1.2); `bash tests/run_sync_tests.sh` → 29/29 (coverage matrix 47/47); `bash tests/e2e/test_smoke_sync_managed_skills.sh` → 70/70 (real-script regression — confirms no false `[ERROR]` on the real 34-entry repo); `tests/run_e2e_tests.sh` 10/10; `tests/test_*.js` 8/8; `validate-agents-md.sh` 11/11; `bash -n scripts/*.sh` clean.
- Avoid: Do NOT refactor the staging pipeline, `generate_command_skill`, or the per-entry loop — the guard is the only staging change. Do NOT change ARCH-004/SPEC-002/milestone (implementation-aligned, not a spec change). Do NOT broaden beyond ERR-1.2.
- Process Note: moved issue #24 Workflow State → `In Review` (option `3cbc15c8`) via `gh project item-edit --project-id PVT_kwDOAGcCyM4Bdw3L --field-id PVTSSF_lADOAGcCyM4Bdw3LzhYQD90 --single-select-option-id 3cbc15c8` (same item ID `PVTI_lADOAGcCyM4Bdw3Lzg046wg` as loop 1). `gh project item-edit` prints nothing on success but exits 0; verify via GraphQL `ProjectV2ItemFieldSingleSelectValue` (note: type name is `SingleSelect`, NOT `SingleOption`).
- Related Docs: SPEC-002 ERR-1.2, ARCH-004 failure modes, PR #28 comment 5166090525 (re-review handover), issue #24 comment 5163449843 (tech-lead decision), milestone #2.



- Context: SPEC-002-T2 — wire `scripts/sync-company.sh` to invoke the new `scripts/sync-managed-skills.sh` (#22/PR #26) as a post-canonical-install step, forward `--force` exactly/only when supplied, and propagate exit codes per CLI-2.4/INT-3.2. 1-file change (+25/−5); `init-company.sh`/`update-company.sh` untouched (CLI-3.1, they `exec`).
- Implementation Lesson (CLI-2.4 exit-code propagation is FREE under `set -euo pipefail`): the canonical install and the managed sync are plain sequential commands under the script's existing `set -e`. A canonical failure exits before the managed call is reached (INT-3.2 — verified `~/.agents/skills` is never created); a managed failure exits `sync-company.sh` with the SAME code (verified exit 5 matches `sync-managed-skills.sh` alone). Do NOT wrap the managed call in `|| exit` or a condition — that MASKS `set -e` and breaks propagation. The `trap 'rm -rf "$temp_dir"' EXIT` does not interfere because it never calls `exit`. Smallest correct mechanism = a bare trailing invocation.
- Implementation Lesson (optional-flag forwarding via empty array): accumulate passthrough flags in `managed_args=()` and call `"$script_dir/x.sh" "${managed_args[@]}"`. The empty-array expansion is SAFE under `set -u` (passes zero args, no empty-string leak) and avoids word-splitting/`[ "$x" ] && ...` gymnastics. This is the right pattern for "forward `--force` only when supplied" / "no new `--dry-run`".
- Implementation Lesson (verifying canonical byte-identity vs pre-SPEC-002 OLD script): the OLD `sync-company.sh` resolves `script_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"` and `source_dir="$script_root/.opencode"`. If you extract OLD to a bare temp path and run it, `source_dir` points at a non-existent `.opencode` and it exits 1 ("Missing source directory"). Build the OLD layout: `$TMP/scripts/sync-company.sh` + `ln -s $WT/.opencode $TMP/.opencode`, then OLD and NEW both read the real source. The "Synced ..." line then differs only in the source_dir VALUE (run from different roots), not the format — normalize both source and target paths before `diff`.
- Process Lesson (zsh tool + exit-code capture): the opencode Bash tool runs under ZSH, where `$pipestatus` is lowercase/1-indexed and `PIPESTATUS[1]` is unset. For exit-code-after-pipe, either avoid the pipe (redirect to a file, then read `$?` directly) or run the whole check inside `bash -c '... set +e; cmd; rc=$?; set -e ...'`. Capturing canonical-fail / managed-fail codes needs `set +e` around the failing command, then re-`set -e`.
- Files/Modules: `scripts/sync-company.sh` only — `script_dir` var + `FORCE=0` + `--force` case branch + trailing managed invocation + CLI-2.2 usage text. Canonical install block (lines through `echo "Synced ..."`) byte-identical to `origin/master`.
- Verification (temp HOME; real `~/.config/opencode` + `~/.agents` never touched): `bash -n` clean; validate-agents-md 11/11; `tests/test_*.js` 8/8; `tests/run_e2e_tests.sh` 10/10; default run → 34 managed + canonical populated + manifest + exit 0; `diff -r .opencode ~/.config/opencode` identical; NEW-vs-OLD canonical tree `diff -r` identical + "Synced" line format byte-identical; tamper→default preserves/`--force` overwrites; canonical-fail→exit 1 no managed dir; managed-fail→exit 5 propagated; wrappers unchanged + delegate `--force` through `exec`.
- Avoid: Do NOT add `--dry-run` passthrough (CLI-2.3 MVP exclusion). Do NOT edit the canonical install block, its output, or error handling (byte-identical guardrail). Do NOT modify `init-company.sh`/`update-company.sh` (CLI-3.1). Do NOT add tests in this issue (#24) or docs (#25).
- Process Note: Issue #23 project item ID `PVTI_lADOAGcCyM4Bdw3Lzg046wk` (workflowState field `PVTSSF_lADOAGcCyM4Bdw3LzhYQD90`, in-progress=`430925ac`, in-review=`3cbc15c8`, ready-to-merge=`894e062a`, done=`f46ff79d`); `gh project item-edit` needs `--project-id PVT_kwDOAGcCyM4Bdw3L`. As with prior SPEC-002/#22 work, main checkout has uncommitted docs/DOCUMENT_INDEX.md + untracked docs (ARCH-004/SPEC-002 are local-only, committed by #25), so worktree created directly via `git worktree add -b feat/spec-002-t2-sync-company-integration ~/Projects/worktree/ant-teams/issue-23 origin/master`.
- Related Docs: `docs/spec/SPEC-002-...md` (FR-12.2, FR-12.3, CLI-2.1..2.4, CLI-3.1, INT-3.2, AC-1.1, AC-2.1, AC-5), `docs/arch/ARCH-004-...md` (Component 2 — sync-company.sh coordination contract), PR #27, issue #23 comment 5158366337 (builder handover).

### 2026-08-02 - SPEC-002 / #22 (PR #26, loop 4)

- Context: Review loop 4 on the managed-sync engine. Two findings: (1) nested symlink-to-dir ancestor escapes the managed subtree (SEC-5.2); (2) update permission policy diverged from SEC-3.2 (used `& 0755` floor, kept 0777 at 0755). Fixed at `b27bc4e`; smoke 70/70.
- Implementation Lesson (SEC-5.2 ancestor symlink): `[[ -d "$cur" ]]` FOLLOWS symlinks, so a symlink-to-dir ancestor passes a directory check and `mkdir -p`/`cp` then write into the symlink target — escaping `~/.agents/skills`. The ancestor walk must reject ANY symlink: `[[ -L "$cur" ]] || [[ -e "$cur" && ! -d "$cur" ]]`. The boundary is layered: entry-level `-L $entry_dir` (top), `target_path_blocked` per-file ancestor walk (middle), `write_target_file`'s `-L $tgt` (file). All three must reject symlinks.
- Implementation Lesson (SEC-3.2 vs SEC-3.1 scoping): SEC-3.1 governs the INSTALL action (fresh/force → source-derived 0644/0755); SEC-3.2 governs the UPDATE action (preserve prior mode unless MORE permissive than 0644 → tighten to exactly 0644). The literal SEC-3.2 reading is correct; any `& 0755` floor diverges. Tighten mask: `extra=$(( prior_dec & ~0644 )); if [[ $extra -ne 0 ]]; then chmod 0644; else chmod $prior; fi`. Consequence accepted: updating an executable script strips x-bit until next force/fresh (0755→0644 on update, restored by force). No spec/ARCH conflict — founder chose the literal reading.
- Implementation Lesson (bash vs zsh octal arithmetic): The opencode Bash tool runs under **ZSH**, where `$((0600))` == 600 DECIMAL. The script's `#!/usr/bin/env bash` parses `0600` as OCTAL 384. So `$(( 0$prior_oct ))` works correctly in the script but a ZSH-run arithmetic sanity check gives wrong values. ALWAYS verify shell arithmetic with `bash -c '...'` (matching the target shebang), not the tool shell, before trusting hand-computed masks.
- Files/Modules: `scripts/sync-managed-skills.sh` (`target_path_blocked`, `write_target_file`, skip message), `tests/e2e/test_smoke_sync_managed_skills.sh` (F3c, F5).
- Verification: `bash tests/e2e/test_smoke_sync_managed_skills.sh` → 70/70; reviewer loop-4 repro (`scripts` dir → symlink-to-external) → 0 files escaped; `0777→0644` update confirmed; `for f in tests/test_*.js; do node "$f"; done` → 8/8; `tests/run_e2e_tests.sh` → 10/10.
- Avoid: Do NOT use `-d` alone to vet a managed ancestor path — it follows symlinks. Do NOT use a `& 0755` floor for SEC-3.2 — it under-tightens (0777→0755 instead of 0644).
- Process Note: Same project item ID `PVTI_lADOAGcCyM4Bdw3Lzg046wU`; in-review option `3cbc15c8`. Posting a PR comment with a `--body` containing backticks/`$()`/parens fails under the zsh tool — write the markdown to a file and use `gh pr comment --body-file`.
- Related Docs: SPEC-002 (SEC-3.1, SEC-3.2, SEC-5.2, FR-11.4), ARCH-004 (Developer Guardrail 1, Failure Modes "Symlink in target path"), PR #26 comment 5156076864 (loop-4 handover), commit `b27bc4e`.

### 2026-08-02 - SPEC-002 / #22 (PR #26, loop 1)

- Context: SPEC-002-T1 — implement the managed `~/.agents/skills` sync engine as ONE bash script `scripts/sync-managed-skills.sh` (per KISS guardrail: no library split, no abstraction layer). Owns FR-2–FR-11, DM-1–DM-4, CLI-1, SEC-1–SEC-5, OBS-1–OBS-4, ERR-1–ERR-6, TR-1–TR-5. Follows ARCH-004 data flow exactly. Tests (#24), sync-company.sh integration (#23), docs (#25) explicitly OUT of scope.
- Implementation Lesson (awk `exit` runs END — critical idempotency bug): `awk '$1==n {exit 0} END {exit 1}'` ALWAYS returns 1 because `exit` in the main block triggers END, whose `exit 1` overrides. On the idempotent re-run every managed entry was misclassified as an unmanaged collision (all `[SKIP collision]`) because `is_managed_name` always returned "not found". Correct pattern: `awk 'BEGIN{f=1} $1==n {f=0; exit} END{exit f}'` — set a flag, decide the exit status ONLY in END. Any awk one-liner that combines an in-body `exit expr` with an `END {exit expr}` has this hazard.
- Implementation Lesson (name-collision double-write): resolving FR-11.1 by only dropping the colliding name from the ENTRY list is NOT enough — the per-file iteration reads the raw source-inventory TSV which still contains BOTH the winning source-skill row and the losing command-derived row for that name, so the generated command SKILL.md clobbers the source-skill SKILL.md (last write wins), and `[INSTALL]` prints twice. Fix: build a filtered `ACTIVE_SOURCE_FILES_TSV` that drops `type==command_derived` rows whose name is in the collision set, and use it for EVERYTHING (entry-type lookup, readability check, file iteration). Entry-name dedup alone is insufficient; filter at the row level.
- Implementation Lesson (OBS-1.1 stream routing is explicit): the spec table assigns `[INSTALL]`/`[UPDATE]`/`[NOOP]` → stdout and `[SKIP modified]`/`[SKIP orphan]`/`[SKIP collision]`/`[FORCE overwrite]`/`[WARNING]`/`[ERROR]` → stderr. A single `say` helper for all action lines is wrong; need `say` (stdout) + `say_err` (stderr). When action lines are buffered in a temp file for NOOP aggregation, route at emission time by prefix: `case "$_line" in "[INSTALL]"*|"[UPDATE]"*) say ... ;; *) say_err ... ;; esac`.
- Implementation Lesson (node as JSON I/O for bash, no new runtime): manifest validate/serialize (DM-4.2 needs real JSON validation for corrupt-detection) is done with a small inline node helper (`manifest_io.js` written to temp dir, invoked `read`/`write` modes, TSV wire format on stdout/stdin). `node` is ALREADY a repo runtime dep (`sync-company.sh` requires it), so TR-1.1 ("no additional runtimes") honored. Missing node → exit 4. Avoid manual JSON parsing (can't reliably detect corruption) and avoid jq (not a guaranteed dep).
- Implementation Lesson (manifest determinism for FR-10 idempotency): the node writer must emit `managed_entries` keys sorted, file keys sorted, fixed top-level order (version, source_repo, last_sync, managed_entries), AND carry forward `installed_at` unchanged for NOOP/orphan entries (only bump it for INSTALL/UPDATE/FORCE). Then two idempotent runs produce byte-identical JSON except the `last_sync` line — verified by `diff <(grep -v last_sync m1) <(grep -v last_sync m2)`.
- Implementation Lesson (per-entry NOOP aggregation): emit ONE `[NOOP] <name> (all N file(s) unchanged)` line per entry when every file is NOOP; otherwise emit per-file lines for non-NOOP files only. Track `entry_files_total` and `entry_files_noop` counters during the file loop, compare after.
- Implementation Lesson (set -euo pipefail + find in while loop): `find ... | while read` creates a SUBSHELL so counters/arrays inside don't persist. Use process substitution `while IFS= read -r f; do ...; done < <(find ...)` to stay in the current shell. For collecting a TSV, wrap the whole `for`+`while` in `{ ... } > file` (current shell, redirect). `set -e` does NOT trigger on the `while read` EOF return.
- Implementation Lesson (ERR-5.1a vs FR-7.3 tension): the spec's partial-run content-recovery (ERR-5.1a: "if dir matches source name, verify content") conflicts with FR-7.3 ("never inspect unmanaged content"). The tech-lead guardrail ("only check directory existence for collision detection; never inspect unmanaged content") RESOLVES it: existing-dir-not-in-manifest is always `[SKIP collision]`, never content-inspected. The managed-file-missing-but-in-manifest case is still auto-restored (`[UPDATE] ... restored missing managed file`) since that path IS manifest-owned.
- Files/Modules: `scripts/sync-managed-skills.sh` (new, 775 lines incl. comments; single bash script + inline node manifest_io helper via heredoc). Only file changed.
- Verification (temp HOME; real `~/.agents/skills` never touched): fresh install → 34 entries (26 source_skill + 8 command_derived), 34 SKILL.md; `diff -r` of multi-file skills identical; 82/82 manifest hashes match files; idempotent re-run all NOOP + manifest identical except last_sync; modify→default preserves `[SKIP modified]` exit 0; `--force` restores `[FORCE overwrite]`; unmanaged sibling untouched across all modes; `--dry-run` writes nothing (`find -newer`=0); synthetic source-vs-command collision → source wins; corrupt manifest → `.corrupt.<ts>` recovery; malformed frontmatter → empty desc; `--force --dry-run` combo exit 0; shasum fallback matches sha256sum; perms 0755/0644; usage→exit 1; perf dry-run ~840ms/full ~920ms; `validate-agents-md.sh` 11/11; `tests/test_*.js` 8/8; `bash -n` clean.
- Avoid: Do NOT add a `--target-dir`/env override to `sync-managed-skills.sh` (spec CLI-1.2 lists only `--force`/`--dry-run`); tests override `$HOME` instead (TR-1.3 resolves `~` via `$HOME`). Do NOT absorb #23 (sync-company.sh `--force` passthrough), #24 (test suite), or #25 (docs) — separate issues. Do NOT implement ERR-5.1a content-inspection recovery (guardrail overrides). Do NOT add tests in this issue (explicitly out of scope).
- Process Note: Issue #22 project item ID is `PVTI_lADOAGcCyM4Bdw3Lzg046wU` (workflowState field `PVTSSF_lADOAGcCyM4Bdw3LzhYQD90`, options: ready=`3cd868ce`, in-progress=`430925ac`, in-review=`3cbc15c8`, ready-to-merge=`894e062a`, done=`f46ff79d`). Main checkout had uncommitted docs/DOCUMENT_INDEX.md + untracked docs (stale vs origin/master) so worktree created directly via `git worktree add -b feat/spec-002-t1-managed-sync-engine ~/Projects/worktree/ant-teams/issue-22 origin/master` (same workaround as prior issues — `create_task_worktree.sh` needs a clean main tree). Issue comment posting via `jq -n --rawfile body <file> '{body:$body}' > payload.json` then `gh api --input payload.json` (resurfaces the backtick/newline JSON-escape lesson).
- Related Docs: `docs/spec/SPEC-002-managed-global-skill-sync-and-command-derived-skills.md` (FR-2..11, DM-1..4, CLI-1, SEC-1..5, OBS-1..4, ERR-1..6, TR-1..5, AC-1..10), `docs/arch/ARCH-004-managed-skill-sync-architecture.md` (Component 1–4, data flow, guardrails), PR #26, issue #22 comment 5155830101 (builder handover).

### 2026-08-01 - SPEC-001 / #10 (PR #21, loop 1)

- Context: SPEC-001-T9 — documentation-only update so `project-initialization/SKILL.md`, README, and DOCUMENT_INDEX accurately reflect the validated init behavior shipped by #2–#9 (per SPEC-001 INT-3.2 / ARCH-003). No code, no tests. The old SKILL.md "Bundled Tools" section still described the legacy `setup_project_docs.sh`/`agent.md` flow; README said init "copies company docs"; DOCUMENT_INDEX lacked SPEC-001, ARCH-003, and all new script/test entries.
- Implementation Lesson (SKILL.md rewrite, guardrail AC-T9-005): replace the OUTDATED section body in place while preserving the frontmatter `description` and the `Use whenever...` trigger verbatim — add new capabilities as additions, not replacements. The "Bundled Tools" section is the natural home for the init-engine contract; rewriting it (rather than appending a parallel section) keeps one canonical description instead of two competing ones. Two stray `agent.md` references elsewhere in the file were corrected to `AGENTS.md` (canonical) with `agent.md` noted as the deprecated fallback.
- Implementation Lesson (DOCUMENT_INDEX scope): the documentation-standard table accepts non-doc artifacts. Scripts and test infrastructure are added as `Type: reference` with `REF-NNN` stable IDs (inspect_repo.js → REF-001, validate-agents-md.sh → REF-002, run_e2e_tests.sh → REF-003, tests/fixtures → REF-004, tests/e2e → REF-005, tests/expected → REF-006). `Last Updated` dates come from `git log -1 --format='%ad' --date=short -- <path>` so the index reflects real last-modified dates, not the index-edit date.
- Critical discovery (untracked canonical docs): SPEC-001 (`docs/spec/`) and ARCH-003 (`docs/arch/ARCH-003-...md`) were NEVER committed to master — every implementation issue #2–#9 referenced them, builders read them, but they lived as untracked working-tree files throughout. PR #20 (self-init, issue #11) only committed the 4 generated artifacts (`.github-project.json`, `.opencode/.gitignore`, `.opencode/opencode.json`, `AGENTS.md`). Verify with `git log --all --oneline -- <path>` (empty = never tracked). Issue #10 requires DOCUMENT_INDEX entries for both (AC-T9-003), so committing the unchanged canonical docs (dated 2026-07-18) alongside the index is the smallest reconciliation that avoids dead-link entries. This is documentation reconciliation, NOT self-initialization (which is about running init-project).
- Implementation Lesson (path-link validation): when validating a DOCUMENT_INDEX, check ONLY the `Path` column (col 6 via `awk -F'|' 'NR>2 {p=$7; ...}'`), not a blanket grep — the `Applies To` and `Related Docs` columns contain comma-separated path fragments (e.g. `scripts/init-project.sh, scripts/init-project-docs.sh`) that produce false "MISS" hits under a naive `grep -oE`.
- Files/Modules: `.opencode/skills/project-initialization/SKILL.md` (Bundled Tools rewrite + 2 agent.md→AGENTS.md fixes), `README.md` (Start Here step 2 + Install Model bullets + validate-agents-md pointer), `docs/DOCUMENT_INDEX.md` (+8 rows: ARCH-003, SPEC-001, REF-001..006), `.opencode/skills/project-initialization/assets/agent-md-template.md` (+1 deprecation HTML comment line 1), `docs/spec/SPEC-001-...md` (new, committed), `docs/arch/ARCH-003-...md` (new, committed). 6 files, +1211/−33.
- Verification: `bash scripts/validate-agents-md.sh AGENTS.md` → 11/11; `for f in tests/test_*.js; do node "$f"; done` → 8 suites, 466/466; `tests/run_e2e_tests.sh` → 10/10 (core=9, smoke=1); `bash -n` clean; all DOCUMENT_INDEX Path-column entries resolve; issue-body greps (DEPRECATED/validate-agents-md/run_e2e_tests/AGENTS.md/inspection) all pass.
- Avoid: Do NOT modify ARCH-003 guardrails/schema in a docs-only issue — its `Last Updated` stays 2026-07-18 (the issue's "only if guardrails changed" qualifier). Do NOT add `~/.agents` or global-install follow-on content. Do NOT delete the deprecated `agent-md-template.md` (ARCH-003 backward-compat contract: deprecate, don't remove).
- Process Note: Issue #10 project item ID is `PVTI_lADOAGcCyM4Bdw3LzgzSC3s` (workflowState field `PVTSSF_lADOAGcCyM4Bdw3LzhYQD90`, options: ready=`3cd868ce`, in-progress=`430925ac`, in-review=`3cbc15c8`, ready-to-merge=`894e062a`, done=`f46ff79d`). Direct `gh api graphql` mutation works; helper script still shadowed by stale `.github-project.env`. As with prior issues, main checkout has untracked docs/memory + docs/spec + docs/arch so the worktree was created directly via `git worktree add -b feat/spec-001-t9-skill-docs-update ~/Projects/worktree/ant-teams/issue-10 origin/master` (the bundled `create_task_worktree.sh` requires a clean main tree).
- Related Docs: `docs/spec/SPEC-001-init-project-tailored-repo-bootstrap.md` (INT-3.2), `docs/arch/ARCH-003-project-local-initialization-artifacts.md` (DM-1/DM-2, backward-compat contract), PR #21, issue #10 comment 5150298538 (builder handover).

### 2026-07-31 - SPEC-001 / #8 (PR #19, loop 2 + resume after interruption)

- Context: Reviewer returned 3 blocking findings on PR #19 loop 1. Builder reworked all three in commit `0d5e2b8` (test-only; no production code). The rework session was then interrupted by the server; on resume I verified the work was already committed/pushed/handed-over and only re-ran verification + posted a resume-confirmation (no duplicate edits/commits).
- Implementation Lesson (vacuous-idempotency anti-pattern, loop-1 finding #1): an idempotency test that hashes the SAME file twice AFTER run 2 proves nothing (identical-time hashes never differ). Correct shape: snapshot AFTER run 1 → optional tamper-sentinel (mutate, assert hash changes, restore, assert hash matches baseline — proves the helper is non-vacuous) → run 2 → snapshot AFTER run 2 → assert run1-snapshot == run2-snapshot for full tree + key artifacts. Also move per-run logs UNDER `$TMP/.git/` (excluded from `e2e_snapshot_files`) so they don't contaminate the run1→2 comparison.
- Portability Lesson (SUPERSEDES loop-1 line 10 `sort -z` guidance): `sort -z` (NUL-separated) is GNU-coreutils ONLY and fails on BSD/macOS `sort` — a TR-1.1 violation. Use `find . -type f -not -path './.git/*' -exec sha256sum {} + 2>/dev/null | sort` instead. POSIX `find -exec ... {} +` batches; the output is `<hash>\t<path>` TEXT lines, so plain `sort` (no `-z`) gives a deterministic stream reproducible across Linux/macOS/FreeBSD. Assumption: no path contains an embedded newline (true for all fixtures + ant-teams). Do NOT reintroduce `sort -z` / `-print0 | sort -z | xargs -0`.
- Implementation Lesson (smoke false-confidence, loop-1 finding #3): a TEST-5.1 smoke that runs `--dry-run` against a `git archive HEAD` temp copy MISSES mutations to untracked/ignored init artifacts (git archive excludes untracked), and conditionally skipping `.github-project.json` lets malformed config pass. Correct shape: exercise the LIVE checkout (`--project-dir $REPO_ROOT`) under strict `--dry-run`, set `--worktree-root` to a tmp path OUTSIDE the live checkout, and prove zero mutation via independent layers: (1) `git status --porcelain` before/after, (2) `git ls-files -z | xargs -0 sha256sum` of tracked files, (3) `git ls-files --others --exclude-standard` list, (4) direct sha256 of every init-managed artifact (AGENTS.md, .github-project.json, `.opencode/skills/` tree) — independent of git so untracked/ignored paths aren't silently missed, (5) DM-1 JSON schema validation of `.github-project.json` when present (required top-level keys + `project.number`) + re-validate JSON after dry-run.
- Process Lesson (resume after server interruption): an interrupted session may have ALREADY committed, pushed, AND posted the handover comment before dying. On resume, FIRST inspect actual state — `git status`, `git log`, `git rev-parse HEAD` vs `origin/<branch>`, board workflow state, and existing PR/issue comments — BEFORE editing or posting. If complete, do NOT duplicate edits, commits, or the detailed handover; instead re-run verification fresh and post only a CONCISE resume-confirmation (additive evidence + state check) pointing to the existing handover for detail.
- Files/Modules: `tests/e2e/test_e2e_idempotent.sh`, `tests/e2e/lib/test_helpers.sh`, `tests/e2e/test_smoke_ant_teams_dry_run.sh` (only these 3 test files changed in `0d5e2b8`).
- Verification: `tests/run_e2e_tests.sh` → exit 0, 10/10 scripts, 131 assertions. `for f in tests/test_*.js; do node "$f"; done` → 8/8 files, 466/466 assertions. `bash -n` clean.
- Related Docs: PR #19 (loop-2 handover + resume confirmation), issue #8 loop-2 summary comment, `docs/spec/SPEC-001-...md` (AC-SPEC-005/008/009, TR-1.1), `docs/arch/ARCH-003-...md`.

### 2026-07-21 - SPEC-001 / #8 (PR #19, loop 1)

- Context: SPEC-001-T7 — end-to-end + migration + smoke test harness (TEST-2.1 seven e2e cases, TEST-3.1/.2 two migration cases, TEST-5.1 ant-teams dry-run smoke) for the completed init pipeline (#2–#7/#9 all merged). Issue guardrails require SHELL-script e2e (not Node) at `tests/e2e/test_<name>.sh` + `tests/run_e2e_tests.sh`; each script standalone with `set -euo pipefail` + `mktemp -d` + trap; no test infra outside `tests/`. No new external deps.
- Implementation Lesson (interactive e2e WITHOUT `expect`/Node PTY): `init_project_docs.sh --interactive` forces interactive mode regardless of TTY (mode resolution = CLI flag → env → `[[ -t 1 ]]` default), and `safe_read` uses `if ! IFS= read -r input; then printf '\n'; fi` which tolerates EOF cleanly under `set -e`. So an interactive e2e test is just `printf '\n\n\n\n\n\ny\n' | bash $INIT --interactive …` — 6 FR-3.2 prompts + 1 confirmation = 7 lines. TEST-2.2 explicitly allows the plain-shell path; the issue flagged `expect` as fragile. This avoids all PTY machinery. Verified: exit 0, AGENTS.md created, all 3 skills copied.
- Implementation Lesson (repo-root resolution off-by-one): a test at `<repo>/tests/e2e/test_*.sh` resolving its repo root via dirname must go `../..` (tests/e2e → tests → repo), NOT `../../..`. The bug manifests as `cp: cannot stat '<wrong>/tests/fixtures/.../.': No such file or directory` and `set -e` turns the cp failure into exit 127 inside the test, so every test reports "expected exit 0 got 127". Always compute levels-up from the actual file location, not from a memorized sibling pattern.
- Implementation Lesson (dry-run zero-change assertion): use `find . -type f -not -path './.git/*' -print0 | sort -z | xargs -0 sha256sum | sha256sum` as a content snapshot, NOT `git diff --exit-code`. E2E fixtures only carry a `.git` directory MARKER (a real .git cannot be tracked inside a fixture per issue #2 builder memory), so there is no commit history for `git diff` to compare against. The content-snapshot hash is portable (coreutils), works on a marker-only repo, and catches any file create/modify/delete. Put any test-managed log files OUTSIDE the snapshot dir (or exclude them) so they don't pollute the before/after comparison.
- Implementation Lesson (TEST-3.2 faithful exercise): the "old `setup_project_docs.sh` after new init must not damage new artifacts" test should run against `repo-legacy-init` upgraded with `--migrate-agent-md`, NOT against a fresh new-init repo. Reason: on a fresh new-init repo there is NO lowercase `agent.md` (new init creates `AGENTS.md` only), so the old script's `copy_if_missing` would CREATE `agent.md` — benign legacy coexistence but it does not exercise the spec's "detects existing files and skips them" path. Starting from `repo-legacy-init` (which ships `agent.md` + legacy `.github-project.json`) forces the old script into the "Exists …" skip branch for BOTH files, which is what TEST-3.2 actually asserts.
- Implementation Lesson (TEST-5.1 ant-teams smoke safety): the smoke must NEVER touch the real ant-teams working tree. Materialize a clean copy via `git archive HEAD | tar -x -C "$TMP"` + `mkdir -p "$TMP/.git"` marker, then run `--dry-run` there. Note: at b2ff793, `.github-project.json` is UNTRACKED in ant-teams (pending #11 self-init), so `git archive HEAD` excludes it. Write the undamage assertion CONDITIONALLY (`if [[ -f $TMP/.github-project.json ]]`) so it activates automatically once #11 commits the file, and always assert the committed `.opencode/skills/` source tree is undamaged + the full tree sha is byte-identical after dry-run.
- Implementation Lesson (#5 exec-bit edge case coverage): the issue #8 collaboration record (comment from #5) asked for a migration smoke check that all required scripts land at mode 755 on a fresh init with fixed source. The cheapest faithful place is INSIDE every e2e test that already exercises a fresh skills copy — assert `assert_exec` on all 5 required scripts (`gh_project_helper.sh`, `create/cleanup_task_worktree.sh`, `init_project_docs.sh`, `setup_project_docs.sh`). One assertion block, reused across tests, no separate "migration smoke" script needed.
- Files/Modules: `tests/run_e2e_tests.sh` (runner; 9 core + 1 smoke, exit 0 iff all pass), `tests/e2e/lib/test_helpers.sh` (shared `set -e`-tolerant helpers: assertions, `e2e_make_fixture_repo`, `e2e_snapshot_files`), 7 e2e + 2 migration + 1 smoke scripts. 12 files, +1014 lines, ALL under `tests/`. Zero production files modified.
- Verification: `tests/run_e2e_tests.sh` → 10/10 scripts, 125 assertions, exit 0. `for f in tests/test_*.js; do node "$f"; done` → 466/466 (no regression on #2–#9). `bash -n` clean on all 12 new scripts. Each `tests/e2e/test_*.sh` independently runnable (standalone guardrail).
- Avoid: Do NOT absorb issue #9 (validator — already merged), #10 (skill + docs update), or #11 (self-init) into this PR — all are parallel/sequential per the milestone sequencing. Do NOT modify any production file; if a test reveals a genuine in-scope defect, record it durably (issue comment + role memory) rather than silently fixing it mid-test-PR. None was found here.
- Avoid: Do NOT use `expect`/Tcl or a Node pseudo-TTY wrapper for the interactive e2e test when `--interactive` + piped stdin works — the user instruction was explicit ("use portable Node pseudo-TTY only if necessary"). Adding a PTY layer would be unnecessary complexity and a fragile test.
- Avoid: Do NOT write the test-runner log files inside the temp dir being snapshot-diffed (dry-run test) — they create a false "file set changed" signal. Use a separate `LOG=$(mktemp)` outside the fixture.
- Process Note: Issue #8 project item ID is `PVTI_lADOAGcCyM4Bdw3LzgzSC2s` (workflowState field `PVTSSF_lADOAGcCyM4Bdw3LzhYQD90`, options: ready=`3cd868ce`, in-progress=`430925ac`, in-review=`3cbc15c8`, ready-to-merge=`894e062a`, done=`f46ff79d`). Direct `gh api graphql` mutation works cleanly (helper script still shadowed by stale `.github-project.env`).
- Process Note: Posting an issue comment whose body contains backticks/quotes/newlines via `gh api -X POST .../comments --input <raw-file>` fails with "Problems parsing JSON" (HTTP 400). Use `jq -n --rawfile body <file> '{body:$body}' > payload.json` then `gh api --input payload.json` — the `--rawfile` reads the body literally without re-escaping (same lesson as #6 but resurfaced).
- Related Docs: `docs/spec/SPEC-001-init-project-tailored-repo-bootstrap.md` (TEST-2.1/2.2, TEST-3.1/.2, TEST-5.1, AC-SPEC-002/003/004/005/007/008/009/010/011/012), `docs/arch/ARCH-003-project-local-initialization-artifacts.md` (Artifact 3 exec-bit guarantee, backward-compat matrix), PR #19, issue #8 comment 5033777127 (builder handover).

### 2026-07-21 - SPEC-001 / #9 (PR #18, loop 1)

- Context: SPEC-001-T8 — implement a structural validator for `AGENTS.md` enforcing ARCH-003 DM-2 / SPEC-001 TEST-4.2. Pure bash + grep + sed + awk (NO Node.js in the validator itself, per tech-lead guardrails); the test harness uses Node only for assertion machinery. Six checks: DM-2.1 (line-1 generation comment), DM-2.2 (H2-only headings, reject H1/H3+), DM-2.3 (no empty H2 sections), DM-2.4 (`## Local Configuration Files` section present), AC-T8-006 (every listed path exists on disk), AC-T8-004 (no placeholder text: TODO / fill this in / your-project-name / TBD, case-insensitive).
- Implementation Lesson: For "reject H1 and H3+ but accept H2" detection in a single grep, `grep -nE '^(# |###)'` is correct but subtle. `^# ` requires hash-space (does NOT match `## foo`, which is hash-hash-space). `^###` matches three-or-more hashes (does NOT match `## foo`). The two anchored alternatives together catch exactly the H1 and H3+ classes while leaving `## ` untouched. Verify each branch against a 3-line fixture before trusting it.
- Implementation Lesson: For DM-2.3 "empty H2 section" detection, a single-pass awk with a `flush()` function is the right primitive. State variables `cur_ln` (0 = no open section), `cur_title`, `has_body` (set when any non-blank line lands inside the open section). `flush()` is called on the next `## ` heading, on any other heading (H1/H3+), and at EOF. Emit `cur_ln ":## " cur_title` for failures so bash can split via `${hit%%:*}` / `${hit#*:}` for the file:line report. Whitespace-only body must NOT count — guard with `$0 ~ /[^ \t]/` rather than `length($0) > 0`.
- Implementation Lesson (DM-2.4 + AC-T8-006 split): keep "section present" and "files listed exist" as SEPARATE checks even when their failure modes overlap. When LCF is missing, DM-2.4 fires AND AC-T8-006 fires ("no list items found"). The redundancy is correct (both facts are true) and avoids coupling the checks. Comment at the AC-T8-006 "no items" branch naming DM-2.4 as the owner of the "section absent" failure mode so future readers understand the intentional overlap.
- Implementation Lesson (deviation documentation): when a tech-lead guardrail suggests a specific primitive (`[[ -f ]]` for file existence) that conflicts with the actual generator contract (the generator emits directory entries like `.opencode/skills/foo/` in `## Local Configuration Files`), use the smallest primitive that honors the contract (`[[ -e ]]`) and document the deviation inline at the check site with the spec citation that justifies it. The reviewer can then validate or push back with full context. A 1-character code change beats silently over-rejecting valid output OR silently under-checking.
- Implementation Lesson (backtick bug in bash double-quoted strings): a `fail "... \`path\` ..."` line in a validator script will be silently parsed as command substitution — bash will try to execute the command `path` and emit `line N: path: command not found` to stderr, polluting the failure output. The escape sequences `\\`` and `\` are not reliable inside double quotes. Simplest robust fix: rephrase the message to avoid backticks entirely (e.g. "backticked path list items" instead of `` `- `path`` `` ). Backticks inside single-quoted bash strings (e.g. awk program text) are safe.
- Implementation Lesson (repo-root resolution for path-existence checks): when the validator takes `<path-to-agents.md>` as the only argument and needs to resolve paths listed inside the file, the right primitive is `repo_root="$(cd "$(dirname "$agents_file")" && pwd)"`. This makes the validator CWD-independent — the operator can invoke `scripts/validate-agents-md.sh /tmp/foo/AGENTS.md` from anywhere and paths like `AGENTS.md` and `.github-project.json` resolve against `/tmp/foo/`, not the operator's CWD. Tests should explicitly assert this (run validator from `REPO_ROOT`, point at a temp dir, confirm temp-dir-local files are found).
- Implementation Lesson (test design — known-good fixture): for a validator, include BOTH a static known-good fixture (deterministic, fast, no dependency on other code) AND one end-to-end test that runs the real generator and validates its output. The static fixture pins the validator contract; the e2e test catches drift if the generator format changes without the validator being updated. Together they catch both directions of contract violation.
- Files/Modules: `scripts/validate-agents-md.sh` (new, 270 lines incl. comments; pure bash + grep + sed + awk, no Node.js), `tests/test_validate_agents_md.js` (new, 34 tests covering AC-T8-001..006 + DM-2.2 H1/H3/H4 + hand-edit guardrail + usage + reporting semantics).
- Verification: `node tests/test_validate_agents_md.js` → 34/34. Full regression `node tests/test_*.js` → 436/436 across 6 suites (no regression on #2/#3/#4/#5/#6/#7 work). `bash -n` OK. Issue body verification commands behave as specified.
- Avoid: Do not absorb issue #8 (e2e/migration tests) or #10 (skill + docs update) into this PR — both are parallel/sequential per the milestone sequencing and would inflate scope. Do not modify any existing files — the issue is purely additive (one new script + one new test file).
- Process Note: Issue #9 project item ID is `PVTI_lADOAGcCyM4Bdw3LzgzSC3c` (workflow state field `PVTSSF_lADOAGcCyM4Bdw3LzhYQD90`, options: ready=`3cd868ce`, in-progress=`430925ac`, in-review=`3cbc15c8`, ready-to-merge=`894e062a`, done=`f46ff79d`). Direct `gh project item-edit --id ... --project-id ... --field-id ... --single-select-option-id ...` works cleanly; helper script still shadowed by stale `.github-project.env`. The board's `status` field (Todo/In Progress/Done) is the LEGACY field and stays "Todo" — the board is driven by `workflow State`, which correctly moved Ready → In Progress → In Review.
- Process Note: When the main checkout has unstaged `docs/DOCUMENT_INDEX.md` and untracked docs (ARCH-003, SPEC-001, memory/, communication/) from prior issues, create the issue worktree directly via `git worktree add -b <branch> <path> origin/master` rather than via the bundled `create_task_worktree.sh` — the helper requires a clean main tree (confirmed again, same as issue #5 process note).
- Related Docs: `docs/spec/SPEC-001-init-project-tailored-repo-bootstrap.md` (DM-2.1..2.4, TEST-4.1/4.2, AC-SPEC-006, AC-SPEC-012), `docs/arch/ARCH-003-project-local-initialization-artifacts.md` (Artifact 2, DM-2, guarantee 1 + 3), `docs/memory/architect-memory.md` line 191 (recommended execution order #9 first), PR #18, issue #9 comment 5033445746.

### 2026-07-19 - SPEC-001 / #2 (PR #12, loop 2)

- Context: Reviewer flagged the `repo-bare` fixture as off-spec vs. canonical SPEC-001 TEST-1.2 (fixture had only `README.md`; spec requires `README.md` + `docs/` + `.git/`). Founder direction: treat canonical TEST-1.2 as authoritative, restore the fixture, reconcile issue AC wording via GitHub comment only.
- Implementation Lesson: When a spec fixture list and an issue-level AC contradict each other, do NOT silently pick one. Surface the contradiction (PR description + reviewer note) and prefer the canonical spec unless tech-lead/strategist explicitly amend it. The contradiction is resolved with the smallest durable record: a GitHub issue comment narrowing AC wording, not a spec or issue-body edit.
- Implementation Lesson: A real `.git/` directory cannot be tracked inside a fixture — git silently ignores nested `.git/`. Document the `.git/` representation in the fixture README rather than materializing a misleading placeholder. The `inspect_repo.js` `detectRepoOrigin` already guards fixture remote leakage via `git rev-parse --show-toplevel` equality, so `repo_origin` stays `"not detected"` for fixtures regardless.
- Files/Modules: `tests/fixtures/repo-bare/` (README + `docs/README.md`), `tests/expected/repo-bare.json`, `tests/test_inspect_repo.js` (`testAcT1002BareRepo`).
- Verification: `node tests/test_inspect_repo.js` → 187 passed, 0 failed. Plus the issue #2 verification CLI invocations.
- Avoid: Do not edit the spec or the issue body to resolve an AC contradiction without explicit tech-lead/strategist approval. Builder scope is GitHub comment + code.
- Related Docs: `docs/spec/SPEC-001-init-project-tailored-repo-bootstrap.md` (TEST-1.2), `docs/arch/ARCH-003-project-local-initialization-artifacts.md`, PR #12 comment 5014961169, issue #2 comment 5014962502.

### 2026-07-19 - SPEC-001 / #5 (PR #13, loop 1)

- Context: SPEC-001-T4 — implement FR-7 (script-bearing skills copy) for `init_project_docs.sh`. Exactly three skills must be copied from the source repo `.opencode/skills/` into the target project's `.opencode/skills/`: `github-issues-projects-cli`, `do-task`, `project-initialization`. No other skill may be copied.
- Implementation Lesson: Per-file merge is the right primitive for FR-7.3. Walk the source skill tree with `find ... -type f -print0`, then for each file `[[ -e "$tgt_file" ]]` → skip (preserve), else `cp -p` (preserves source execute bit per SEC-3.2). Do NOT use `cp -Rn` for skills — the issue body explicitly asked for `[[ -f "$target" ]]` per-file checks, and per-file walk is what lets `[writing]` log lines (FR-9.1 / OBS-1) be emitted per copied file.
- Implementation Lesson: `cp` without `-p` does NOT reliably preserve execute bits across umask environments. SEC-3.2 says "no explicit chmod required" — that is satisfied by `cp -p` (a cp flag, not a chmod call). Use `cp -p` everywhere execute bits must travel.
- Implementation Lesson: For customized-SKILL.md preservation tests, assert BOTH content equality AND mtime equality. Content equality alone misses silent "rewrite with identical bytes" regressions; mtime catches them.
- Files/Modules: `.opencode/skills/project-initialization/scripts/init_project_docs.sh` (new `copy_required_skills` + `ensure_opencode_gitignore`, wired in after `ensure_opencode_config`), `tests/test_skills_copy.js` (34 unit tests).
- Verification: `node tests/test_skills_copy.js` → 34 passed, 0 failed. `node tests/test_inspect_repo.js` → 187 passed, 0 failed (no regression).
- Avoid: Do not fix unrelated `cp: behavior of -n is non-portable` warnings emitted by the pre-existing docs-copy line — that belongs in issue #7 (observability/error handling). Same for the source-repo `do-task/scripts/*.sh` missing execute bit; SEC-3.2 only requires preserving whatever the source has.
- Process Note: `gh_project_helper.sh set-status` is shadowed by stale placeholder values in `.github-project.env` (`PROJECT_ID="PVT_kwDOEXAMPLE"` etc.). For board state transitions, call `gh api graphql` directly with the real IDs from `.github-project.json` (`projectId=PVT_kwDOAGcCyM4Bdw3L`, workflowState field `PVTSSF_lADOAGcCyM4Bdw3LzhYQD90`, options: ready=`3cd868ce`, in-progress=`430925ac`, in-review=`3cbc15c8`, ready-to-merge=`894e062a`, done=`f46ff79d`).
- Process Note: When main checkout has unrelated uncommitted files that must be preserved, create the issue worktree directly via `git worktree add -b <branch> <path> origin/master` rather than via the bundled `create_task_worktree.sh` — the helper requires a clean main tree.
- Related Docs: `docs/spec/SPEC-001-init-project-tailored-repo-bootstrap.md` (FR-7, FR-9, SEC-3, AC-SPEC-007), `docs/arch/ARCH-003-project-local-initialization-artifacts.md` (Artifact 3), PR #13, issue #5 comment 5015906169.

### 2026-07-19 - SPEC-001 / #5 (PR #13, loop 2 — execute-bit fix)

- Context: Reviewer HIGH finding on PR #13 review 4730838030: `cp -p` faithfully copied non-executable source scripts (`do-task/scripts/{create,cleanup}_task_worktree.sh`, `project-initialization/scripts/setup_project_docs.sh`) into target at mode 664, violating ARCH-003 Artifact 3 guarantee 4.
- Implementation Lesson: When init lands non-executable scripts, fix the SOURCE mode (100644→100755), not the init path. Source-mode correction is the smallest robust fix because (a) SEC-3.2 already says "no explicit `chmod` call is required" — `cp -p` preserves what the source owns; (b) a post-copy `chmod` in init would violate SEC-3.1 ("must not set explicit permissions beyond what `mkdir -p` and `cp` provide by default"); (c) the source repo is the only canonical layer every consumer (init, tests, future tools) reads from. No SPEC/ARCH doc edit needed — the docs were already correct; the source was the violation.
- Implementation Lesson: Tests for exec-bit invariants must positively iterate the FULL set of required scripts AND assert the bit is set — equality-only checks (`target_mode == source_mode`) against an already-passing file hide source-side regressions. The reconciled `tests/test_skills_copy.js::testAllCopiedShellScriptsExecutable` walks every `.sh` under target required-skill `scripts/` and asserts BOTH positive exec bit AND source/target equality; `testSourcePreflight` does the same at source so the regression is caught before any init runs.
- Implementation Lesson (tooling): JS `/* ... */` docblocks containing literal glob patterns like `skills/*/scripts/` prematurely terminate the block comment via the `*/` sequence — Node parses subsequent lines as code and reports a confusing syntax error far from the actual cause. Use `<skill>` or another placeholder inside block comments; only `*/` outside `//` line comments is dangerous.
- Files/Modules: 3 source mode changes (`100644`→`100755`), `.opencode/skills/project-initialization/scripts/init_project_docs.sh` (rationale comment in `copy_required_skills`), `tests/test_skills_copy.js` (new `REQUIRED_SCRIPT_PATHS`, `listShellScriptsInRequiredSkillDirs`, reconciled suites; 34→53 tests).
- Verification: `node tests/test_skills_copy.js` → 53/53 pass; reverting source modes reproduces 10 failures matching the reviewer finding exactly. `node tests/test_inspect_repo.js` → 187/187 pass. End-to-end smoke + idempotency smoke pass (all 5 required scripts land 755; re-init leaves modes unchanged).
- Avoid: Do not add a `chmod` call inside the init skills-copy path — it violates SEC-3.1 and papers over a source bug. Do not write `*/` glob sequences inside JS block comments.
- Related Docs: `docs/spec/SPEC-001-init-project-tailored-repo-bootstrap.md` (SEC-3.1, SEC-3.2), `docs/arch/ARCH-003-project-local-initialization-artifacts.md` (Artifact 3 guarantee 4), PR #13 comment 5015979264, issue #5 comment 5015980427.

### 2026-07-19 - SPEC-001 / #3 (PR #14, loop 1)

- Context: SPEC-001-T2 — implement CLI flag expansion + env-var resolution + TTY-based mode detection + validation for `init_project_docs.sh` (CLI-2 / FR-4 / ERR-4). Thin wrappers under `scripts/` must stay pass-through; downstream AGENTS.md / config / dry-run behaviors belong to T3/T5/T6.
- Implementation Lesson: When a spec flag table and an issue-body prose count disagree (issue said "14 new flags" but its own CLI-2 enumeration listed 16), trust the canonical spec table. Note the discrepancy in the PR description so reviewer/tech-lead can correct the issue body if needed. Do NOT silently narrow scope to match the prose count.
- Implementation Lesson: Resolution order "default < env < CLI flag" is cleanly implementable as two sequential phases in bash — Phase 1 applies env via `${INIT_PROJECT_*:-default}`, Phase 2 parses CLI into the same variables and overrides. Track explicit-CLI vs default/env separately ONLY for mode (`mode_cli` empty/non-empty) so the TTY default branch (`[[ -t 1 ]]`) can fire when neither env nor CLI forces mode.
- Implementation Lesson: A boolean flag with a mode-dependent default (CLI-2 `--merge`: interactive=on, noninteractive=off) must defer defaulting until AFTER mode is resolved. Initialize to empty string from env (`opt_merge="${INIT_PROJECT_MERGE:-}"`), set to 1 from CLI flag, then after mode resolution apply the default if still empty. Treating empty as "unset" lets `INIT_PROJECT_MERGE=0` work as "explicitly off" instead of being clobbered by the default.
- Implementation Lesson: When adding a noninteractive required-flag check (ERR-4.1) to an existing script, ANY pre-existing test suite that invokes the script with stdout piped will trip the new check and break. Proactively update those suites' invocation helpers to pass the required flags. For T4's `runInit` helper the minimum change was adding `--noninteractive --name test --github-owner antpolis --github-project-number 9` to the argv — the suite's actual skills-copy assertions are unaffected.
- Implementation Lesson (tooling): Testing the TTY-default branch (`[[ -t 1 ]]` → interactive) from a Node child process is not possible via `execFileSync` (stdout is always piped). Use `script -qec "bash <cmd>" /dev/null` on Linux to allocate a pseudo-TTY; detect `script` via `which script` and skip the test cleanly if unavailable. The behavioral observation (no ERR-4.1 missing-flag check fires under pty) is sufficient — there is no need to read internal `mode` state via debug instrumentation.
- Implementation Lesson (validation): `--related-repos name:url:relationship` validation must require "at least 2 colons per triple", NOT exactly 2 — URLs like `https://github.com/...` contain colons. Spec says "stored as-is, never fetched" (SEC-1.3) so shape validation is the only concern, not URL parsing.
- Implementation Lesson: `@file` syntax resolution for `--commands` / `--conventions` belongs AFTER CLI parsing but BEFORE the noninteractive required-flag check, so that `--commands @missing` fails loudly regardless of mode. File-not-found is a hard error (`exit 1`) — silent fallback to empty would mask operator intent.
- Files/Modules: `.opencode/skills/project-initialization/scripts/init_project_docs.sh` (4 new helpers + 3-phase parsing/validation, ~150 lines; usage refresh; version `0.1.0 → 0.2.0`), `tests/test_cli_flags.js` (new, 42 tests covering AC-T2-001..007 + guardrails), `tests/test_skills_copy.js` (runInit updated to pass required noninteractive flags).
- Verification: `node tests/test_cli_flags.js` → 42/42 pass; `node tests/test_skills_copy.js` → 53/53 pass (no regression); `node tests/test_inspect_repo.js` → 187/187 pass (no regression); `bash -n init_project_docs.sh` → OK; all three issue-body verification commands behave as specified.
- Avoid: Do not implement the downstream behaviors of `--dry-run` (OBS-2 file suppression), `--skip-inspection` (skip inspect_repo.js), or `--force` (skills re-copy) when the task is CLI-surface-only — those ship with later issues and wiring the flag parsing now is sufficient. Do not modify the thin delegation wrappers under `scripts/` even to "add helpful comments" — pass-through means literally no source changes.
- Avoid: Do not write to `docs/proj-management/communication/SPEC-001-communication.md` for handovers — the GitHub issue comment is the canonical record (per `github-agentic-delivery-flow` "Source Of Truth Rules"). The local file is a legacy artifact.
- Process Note: Issue #3 project-item ID is `PVTI_lADOAGcCyM4Bdw3LzgzSC0o` (workflow state field `PVTSSF_lADOAGcCyM4Bdw3LzhYQD90`, options: ready=`3cd868ce`, in-progress=`430925ac`, in-review=`3cbc15c8`, ready-to-merge=`894e062a`, done=`f46ff79d`). Direct `gh api graphql` mutation works; the helper script is still shadowed by stale `.github-project.env`.
- Related Docs: `docs/spec/SPEC-001-init-project-tailored-repo-bootstrap.md` (CLI-2, FR-4, ERR-4), `docs/arch/ARCH-003-project-local-initialization-artifacts.md`, PR #14, issue #3 comment 5016082095.

### 2026-07-19 - SPEC-001 / #3 (PR #14, loop 2 — validation tightening)

- Context: Reviewer found two validation gaps in the T2 parser: (1) `--github-project-number nope` exited 0 (CLI-2 types this flag as `Integer`); (2) `validate_related_repos()` only required ≥2 colons per triple, so `a:https://github.com/o/a:parent:extra` (4 colons) was accepted.
- Implementation Lesson: For positive-integer validation in bash, `[[ "$num" =~ ^[1-9][0-9]*$ ]]` is the right primitive — it rejects `0`, `-1`, `1.5`, `1e10`, `0x10`, `07`, leading/trailing whitespace, and non-numeric strings in one check. Avoid `(( num > 0 ))` as the primary check because it silently coerces non-numeric strings to 0 (so `nope` becomes "valid 0"), and because bash arithmetic interprets leading-zero strings as octal. Empty input should pass the validator (return 0) so the missing-flag accounting path owns the "not provided" case with its own clearer error.
- Implementation Lesson: For colon-delimited triple validation in bash, DO NOT use `read -ra parts <<< "$triple"` with `IFS=':'` — bash `read` silently drops trailing empty fields, so `a:url:` parses as `[a, url]` and the empty-relationship case is invisible. Use parameter expansion instead: `name="${triple%%:*}"`, `relationship="${triple##*:}"`, `url="${triple#*:}"; url="${url%:*}"`. This observes every empty field position.
- Implementation Lesson: "Strict `name:url:relationship` validation" does NOT mean "exactly 3 colon-separated parts" — URL fields legitimately carry colons (`https:`, SCP-style `user@host:`). The right rule is: name and relationship are single tokens (no colons, automatic from the parameter-expansion extraction), url is the middle, AND url carries at most one colon. This accepts every documented Git URL form (https, no-scheme, SCP/SSH, local path) while rejecting the reviewer's example (url `https://github.com/o/a:parent` has 2 colons).
- Implementation Lesson (TDD): For review-loop fixes, write the regression test FIRST and run it to confirm red against the previous commit, then implement the fix and confirm green. This proves the test actually exercises the bug and gives the reviewer a binary artifact to verify. Group invalid and valid cases as table-driven arrays so the test reads as a contract spec.
- Implementation Lesson (test design): When the validator delegates the empty case to a different error path (e.g. ERR-4.1 missing-flag accounting), do NOT include `''` in the "invalid value" test list — it will exit 1 (correctly) but with a different stderr message and your regex assertion will spuriously fail. Test the empty case separately against the missing-flag path.
- Files/Modules: `.opencode/skills/project-initialization/scripts/init_project_docs.sh` (new `validate_github_project_number`, rewrote `validate_related_repos`, Phase 3 wiring, usage tweaks), `tests/test_cli_flags.js` (+22 regression assertions in 2 new suites; 42→73 tests total for this file).
- Verification: `node tests/test_cli_flags.js` → 73/73 (was 42/42 pre-loop-2; the 22 new assertions were red against `e0688bb` then green against `dda6416`); `node tests/test_skills_copy.js` → 53/53; `node tests/test_inspect_repo.js` → 187/187; `bash -n` OK; reviewer smoke examples both reproduce as exit 0 against `e0688bb` and as exit 1 with clear stderr against `dda6416`.
- Avoid: Do not over-broaden URL validation with full URL parsers or regex — the SEC-1.3 contract is "stored as-is, never fetched", so we only validate shape, not URL well-formedness. The url-one-colon rule is the smallest check that admits every documented form and rejects the ambiguous-extra-colons class.
- Avoid: Do not add a `case` branch on parts-count alone (`3 parts → ok, 4 parts → ok if scheme, else reject`) — that path rejects legitimate SCP-style URLs like `a:git@github.com:o/a:parent` where parts[1]=`git@github.com` doesn't match a clean scheme regex. Parameter-expansion extraction + url-colon-count handles all forms uniformly.
- Related Docs: `docs/spec/SPEC-001-init-project-tailored-repo-bootstrap.md` (CLI-2 type table line 464: `--github-project-number Integer`; line 462: `--related-repos String (comma-separated name:url:relationship triples)`; SEC-1.3), `docs/arch/ARCH-003-project-local-initialization-artifacts.md` (related_repos schema), PR #14 comment 5016162346, issue #3 comment 5016163289.

### 2026-07-19 - SPEC-001 / #3 (PR #14, loop 3 — url field opacity)

- Context: Reviewer reported that the loop-2 `url carries at most one colon` rule rejected realistic Git remote forms that include a port: `sibling:https://github.com:443/org/repo:sibling` and `sibling:ssh://git@github.com:22/org/repo:sibling`. This conflicted with SEC-1.3 / ARCH-003, which treat the url field as an opaque git remote URL or file path stored as-is. Architect memory already flagged that "validation that bans valid URL shapes is too narrow unless the contract is explicitly narrowed."
- Implementation Lesson: When a spec/ARCH says a field is "opaque" or "stored as-is, never parsed", validation MUST NOT inspect that field's internal structure. The only valid checks are (a) the field is non-empty, and (b) the entry has enough structure around it (delimiters) for the framework to extract it. Any rule that inspects internal characters (colon-count, scheme regex, port patterns) is over-reach that will eventually reject a real-world input the spec allows.
- Implementation Lesson: For `name:url:relationship` parsing where url is opaque, the right algorithm is first-colon / last-colon split, NOT field-counting:
  - `name = "${triple%%:*}"`         (text before first colon)
  - `relationship = "${triple##*:}"`  (text after last colon)
  - `url = "${triple#*:}"; url="${url%:*}"`  (everything in between, verbatim)
  This accepts any url with internal scheme/port/path colons (https://h:443/p, ssh://git@h:22/p, git@h:path, /abs/path, ./rel/path) and rejects only the unambiguous-malformed class: fewer than 2 colons total (no relationship delimiter), empty name, empty url (middle is blank), empty relationship.
- Implementation Lesson (test-suite replacement): When a review loop invalidates a previous rule, do not leave the old rejection tests in place — they will fail under the new rule and mislead future readers. Replace the whole regression suite with one that encodes the new contract, and call out the replacement in the test-header comment so the loop history is visible.
- Avoid: Do not attempt to reject "extra trailing fields" (e.g. `a:b:c:d:e`) when the url field is opaque. The parser cannot know whether the middle colons belong to a port, a path, or an SCP remote; any rejection rule will eventually misfire on a real URL form. Only fail where the failure is unambiguous (empty component, missing delimiter).
- Files/Modules: `.opencode/skills/project-initialization/scripts/init_project_docs.sh` (rewrote `validate_related_repos` to first/last-colon parser; usage() updated), `tests/test_cli_flags.js` (replaced `regression --related-repos strict contract` suite with `regression --related-repos opaque URL contract`; 73→81 tests).
- Verification: `node tests/test_cli_flags.js` → 81/81 (3 of the old suite's assertions were red against `5ba6017` because they encoded the prior rule; the new suite is green); `node tests/test_skills_copy.js` → 53/53; `node tests/test_inspect_repo.js` → 187/187; `bash -n` OK; reviewer's exact port-bearing examples both smoke-test as exit 0; all 9 unambiguous-malformed forms smoke-test as exit 1 with the standard `[error] Invalid --related-repos entry` block.
- Related Docs: `docs/spec/SPEC-001-init-project-tailored-repo-bootstrap.md` (SEC-1.3, line 496), `docs/arch/ARCH-003-project-local-initialization-artifacts.md` (related_repos schema treats url as opaque string), `docs/memory/architect-memory.md` line 90 (url opacity constraint), PR #14 comment 5016214420.

### 2026-07-19 - SPEC-001 / #6 (PR #15, loop 1)

- Context: SPEC-001-T5 — implement ARCH-003 DM-1 canonical `.github-project.json` schema extension (worktreeRoot + identity + boundaries + initMeta) additively, plus FR-6 / SEC-2 backward-compatible migration. Existing fields must be preserved verbatim; rerun must be byte-for-byte idempotent. Also extend `ensure_opencode_config` detection to the canonical `.opencode/opencode.json` location per ARCH-003 Artifact 4.
- Implementation Lesson: The right idempotency primitive for a config file that contains a "last-modified" timestamp is **structural deep-equal with the volatile field stripped from both sides**. Compute desired state via per-field additive merge, `JSON.stringify(parsed, null, 2)` vs `JSON.stringify(desired, null, 2)` with `generatedAt` removed from both — equal → no-op (preserve file byte-for-byte, even the old timestamp), not equal → write desired with refreshed `generatedAt`. Naive approaches break: (a) "always update generatedAt" makes every run a write (breaks idempotency); (b) "set generatedAt only if other changes detected" sounds right but requires two passes. The strip-and-compare trick is a single pass and is the smallest robust primitive.
- Implementation Lesson: For ARCH-003 / DM-1 strict-additive merge in node, the safest shape is `const desired = JSON.parse(JSON.stringify(parsed))` (deep-clone the input) then per-field `if (!desired.x) desired.x = value;`. The deep-clone prevents any mutation of the parsed input, and the per-field `if (!...)` checks make additivity structurally impossible to violate. Type guards (`typeof === "object" && !Array.isArray`) on sub-objects defend against a legacy file where `identity` is a string instead of an object — without them, `desired.identity.name` would throw on a malformed legacy file.
- Implementation Lesson: When the issue ACs reference a file the implementation doesn't yet write (AC-T5-006 references `AGENTS.md` backup, but AGENTS.md generation ships with T3), pin the boundary with a **dormant test** that asserts the current "T5 doesn't touch this" behavior. The test name and comment record WHY the full AC isn't asserted yet, so a future regression that adds the behavior without the contract is caught. This is more durable than a "not implemented" comment.
- Implementation Lesson: `ensure_opencode_config` was looking only at repo-root `opencode.jsonc`/`opencode.json` and missing the canonical `.opencode/opencode.json` location (ARCH-003 Artifact 4). The smallest correct fix is **detection widening only** — check `.opencode/opencode.*` first, fall back to repo root. Do NOT change the default creation target (existing adopters expect `opencode.jsonc` at repo root from prior init runs). Changing both detection AND default creation would silently migrate every fresh init to a new location, breaking backward compat.
- Implementation Lesson (process): When GitHub GraphQL rate limit is exhausted (`remaining: 0`), REST API still works for issue comments (`POST /repos/{owner}/{repo}/issues/{n}/comments`) and PR creation (`POST /repos/{owner}/{repo}/pulls`). `gh pr create` and `gh issue view --comments` both need GraphQL and fail; build the request body with `jq -n --rawfile body /dev/stdin '{...}'` then `gh api -X POST ... --input <file> --jq '.number,.html_url'`. The `--rawfile` directive reads the body literally without re-escaping, avoiding the "Problems parsing JSON" 400 that hand-rolled heredocs hit when the body contains quotes / newlines / backticks.
- Implementation Lesson (process): Project board state transitions need GraphQL even when the issue/PR REST endpoints don't. Do NOT silently skip the state transition when GraphQL is rate-limited — post a durable comment on the issue naming the missing transition ("GraphQL rate limit temporarily prevented moving this issue's project item to In Review; will apply once budget resets") so the canonical record reflects reality. Then actually apply the transition when the budget resets, don't just leave the comment as a plan.
- Process Note: Issue #6 project-item ID can be looked up via GraphQL `query($q: String!) { search(query: $q, type: ISSUE, first: 1) { nodes { ... on Issue { projectItems(first: 1) { nodes { id } } } } } }` with `q: "repo:Antpolis/ant-teams issue:6"` once GraphQL budget is back. The workflowState field ID is `PVTSSF_lADOAGcCyM4Bdw3LzhYQDlE` and the option IDs are ready=`3cd868ce`, in-progress=`430925ac`, in-review=`3cbc15c8`, ready-to-merge=`894e062a`, done=`f46ff79d` (from `.github-project.json`).
- Files/Modules: `.opencode/skills/project-initialization/scripts/init_project_docs.sh` (rewrote `ensure_github_project_config` with node additive merge + structural idempotency; extended `ensure_opencode_config` detection; main-flow call site updated; +~210 lines), `tests/test_github_project_config.js` (new, 36 tests).
- Verification: `node tests/test_github_project_config.js` → 36/36; `node tests/test_inspect_repo.js` → 187/187; `node tests/test_skills_copy.js` → 53/53; `node tests/test_cli_flags.js` → 81/81; `bash -n` OK; all issue-body verification commands pass (jq parse, identity.name resolution, depends_on=[], owner preserved on legacy fixture, byte-for-byte diff on rerun).
- Avoid: Do NOT add a `--depends-on` CLI flag at T5 scope — CLI-2 doesn't define one, and FR-8.4 treats depends_on vs related_repos as semantically distinct (operational vs peer). `boundaries.depends_on` is intentionally always `[]` until a future CLI-2 expansion. All operator-provided triples route to `related_repos`.
- Avoid: Do NOT change the default opencode config creation location from repo-root to `.opencode/` at T5 scope — that is a separate behavior change that would silently affect every fresh init. The issue scope is detection widening + additive external_directory entry, not migration of the default location.
- Avoid: Do NOT treat `initMeta.generatedAt` as "file creation time" or "every run timestamp". It is "last material init write". A no-op rerun must NOT refresh it, or AC-T5-005 / TR-2.1 idempotency breaks.
- Related Docs: `docs/spec/SPEC-001-init-project-tailored-repo-bootstrap.md` (FR-6, FR-8, SEC-2, DM-1, AC-SPEC-004/005/008/011), `docs/arch/ARCH-003-project-local-initialization-artifacts.md` (Artifact 1 schema, Artifact 4 location, guarantee 2 strict additivity), PR #15, issue #6 comments 5016305997 (PR) / 5016307299 (issue link).

### 2026-07-19 - SPEC-001 / #6 (PR #15, loop 2 — rework)

- Context: Two reviewer findings on PR #15: (1) fresh init created legacy repo-root `opencode.jsonc` instead of canonical `.opencode/opencode.json`; (2) malformed `.github-project.json` emitted Node stack trace instead of controlled `[error]`.
- Implementation Lesson: When a reviewer flags a location-contract drift (wrong default creation target for config files), the smallest fix is: change the DEFAULT CREATION PATH only; keep the detection order broad across all 4 supported locations. Do NOT change both detection and default — that silently migrates every existing adopter's config location. The fix added `.opencode/opencode.json` as the creation default with `mkdir -p` for the `.opencode/` directory, while detection order (`canonical.json` → `canonical.jsonc` → `root.jsonc` → `root.json`) was already correct from loop 1.
- Implementation Lesson: Malformed config-file handling at the node layer MUST produce `[error]`-prefixed stderr messages matching the bash-layer convention, never a raw Node stack trace. Wrap `fs.readFileSync` + `JSON.parse` in separate try/catch blocks (not a single block) so the operator knows whether the file couldn't be READ or couldn't be PARSED. On parse failure, add a second message: "Fix the JSON manually and re-run; init will not overwrite a broken file." Both code paths abort BEFORE any mutation — no partial write, no silent corruption.
- Implementation Lesson (TDD): For review-loop fixes, write the regression test FIRST against the broken state to confirm it demonstrates the failure, then fix and confirm green. The reviewer needs a binary artifact (the red-green transition) to verify without reading every line. In this loop, 12 new regression tests under "PR15 review finding 1/2" suites were red against `56b6eec` (loop 1) and green against `cf3e831` (loop 2).
- Files/Modules: `.opencode/skills/project-initialization/scripts/init_project_docs.sh` (`ensure_opencode_config` default creation target; `ensure_github_project_config` try/catch), `tests/test_github_project_config.js` (+12 regression tests under PR15-1 and PR15-2 suites; 36→48).
- Verification: `node tests/test_github_project_config.js` → 48/48 (+12 from loop 1); all other suites unchanged at 187/187, 53/53, 81/81. Smoke: fresh init at `.opencode/opencode.json`, root config preserved, jsonc extension preserved, malformed file controlled error, abort before mutation.
- Avoid: Do NOT add a `chmod` to the `.opencode/` creation — `mkdir -p` covers directory creation; `chmod` would violate SEC-3.1. Do NOT `process.exit(0)` on idempotent no-op; let the node exit handler fire naturally after `No changes needed` log, matching the script's `set -euo pipefail` contract.
- Related Docs: `docs/spec/SPEC-001-init-project-tailored-repo-bootstrap.md`, `docs/arch/ARCH-003-project-local-initialization-artifacts.md` (Artifact 4 guarantee 3), PR #15 comment 5016389700.

### 2026-07-20 - SPEC-001 / #4 (PR #16, loop 1)

- Context: SPEC-001-T3 — the critical-path deliverable. Implement FR-3 (interactive 6-prompt flow), FR-4 (noninteractive flag-driven generation), FR-5 (AGENTS.md artifact contract) for `init_project_docs.sh`. AGENTS.md is plain Markdown, DM-2 structure, every claim traceable to inspection evidence or operator input (FR-5.3), empty sections omitted (DM-2.3), Local Configuration Files always present (DM-2.4), timestamp comment on line 1 (DM-2.1).
- Implementation Lesson: When a bash function both prints to stdout AND needs to return a value, do NOT use `$(function_name)` to capture the return — stdout capture will include the printed text, polluting the return value. Use a global variable instead. This bit `decide_existing_agents_md_action` which printed `[prompt] Action [o/m/s]:` via `safe_read` (stdout) and returned the chosen action via `printf 'overwrite'` (also stdout). The `$()` capture got `[prompt] Action [o/m/s]: overwrite` — a string that matched no case branch. Fix: set a global `AGENTS_MD_ACTION` variable.
- Implementation Lesson: Interactive prompts that `read -r` from stdin will block indefinitely under a pseudo-TTY (`script -qec CMD /dev/null`) because the pty slave never sees EOF. For test compat when T3 adds prompts to a previously-prompt-free interactive path, feed piped stdin input (`\n`.repeat(N) for N prompts + confirm) to the `spawnSync` call. Add a `timeout: 30000` safety net. The previous T2 pty test only checked "no missing-flag error fires" — still valid, just needs stdin.
- Implementation Lesson: A "dormant" boundary test (asserting behavior X is NOT yet implemented at scope N) should convert to an active test when scope N+1 implements X. The T5 dormant test `AC-T5-006` asserted "AGENTS.md is NOT generated" — T3 made it active: "AGENTS.md IS generated with backup on `--force`". This is the designed lifecycle for dormant tests; don't delete them, activate them.
- Implementation Lesson: For AGENTS.md section assembly, the cleanest architecture is: bash handles inspection + prompting + file I/O; an inline node heredoc handles JSON parsing + markdown assembly + merge logic. Env vars pass multi-line operator values safely (argv would need escaping). The node helper reads `process.env.AGENTSMD_*` and writes markdown to stdout; the bash side captures via `$(generate_agents_md_content)` with env vars set inline at the call site.
- Implementation Lesson: For `--force --merge` of a Markdown file with H2 sections, the merge algorithm is: collect existing H2 headings into a Set; split fresh content into sections by H2; append only sections whose heading is NOT in the existing set; preserve existing body verbatim; refresh line-1 generation comment. This is section-level merge (not line-level), which matches FR-5.5 "preserves existing content" at the right granularity.
- Implementation Lesson: `read -r` under `set -euo pipefail` returns false on EOF, which would abort the script. Wrap as `if ! IFS= read -r input; then printf '\n'; fi` (the `if !` form doesn't trigger `set -e`) and use `printf -v "$out_var" '%s' "$input"` to assign by name (safer than eval, works in bash 4.0+). This is the `safe_read` helper.
- Implementation Lesson: Version bumps should happen in the issue that introduces the capability, not retroactively. T3 bumped 0.2.0 → 0.3.0 because AGENTS.md generation is a user-visible capability that `initMeta.version` should reflect (ARCH-003 / DM-1.3). The T5 test that asserted 0.2.0 needed updating — expected and called out in the PR.
- Files/Modules: `.opencode/skills/project-initialization/scripts/init_project_docs.sh` (8 new helpers + main-flow wiring, +757 lines), `tests/test_agents_md_gen.js` (new, 33 tests), `tests/test_cli_flags.js` (pty input compat), `tests/test_github_project_config.js` (version + dormant→active).
- Verification: `node tests/test_agents_md_gen.js` → 33/33; `node tests/test_inspect_repo.js` → 187/187; `node tests/test_skills_copy.js` → 53/53; `node tests/test_github_project_config.js` → 48/48; `node tests/test_cli_flags.js` → 81/81. Total: 402/402. Issue-body verification commands pass (timestamp present, no placeholders, traceable sections).
- Avoid: Do NOT implement `--dry-run` file suppression at T3 scope — that's #7 (OBS-2). The flag is parsed (T2) but does not suppress AGENTS.md writes. This is explicitly deferred per the issue scope.
- Avoid: Do NOT migrate legacy `agent.md` content into `AGENTS.md` at T3 scope — FR-6.1 (active migration via `--migrate-agent-md`) is out of scope. The legacy file is preserved untouched (verified by regression test); only its existence is used as an inspection signal.
- Avoid: Do NOT put the opencode config path detection logic in the AGENTS.md generator AND in `ensure_opencode_config` as separate implementations — they must agree. The AGENTS.md "Local Configuration Files" section mirrors the same detection order (`.opencode/opencode.json` → `.opencode/opencode.jsonc` → root `opencode.jsonc` → root `opencode.json`) so the path listed matches the path actually written.
- Process Note: Issue #4 project item ID is `PVTI_lADOAGcCyM4Bdw3LzgzSC1I` (workflowState field `PVTSSF_lADOAGcCyM4Bdw3LzhYQD90`, options: ready=`3cd868ce`, in-progress=`430925ac`, in-review=`3cbc15c8`, ready-to-merge=`894e062a`, done=`f46ff79d`). The Status field (`PVTSSF_lADOAGcCyM4Bdw3LzhYQDlE`) is a legacy Todo/In Progress/Done field with different option IDs (in-progress=`47fc9ee4`). Both must be updated for the board to show consistent state.
- Related Docs: `docs/spec/SPEC-001-init-project-tailored-repo-bootstrap.md` (FR-3, FR-4, FR-5, DM-2, AC-SPEC-002/003/006/011/012), `docs/arch/ARCH-003-project-local-initialization-artifacts.md` (Artifact 2 AGENTS.md contract, DM-2 structure, ERR-3.2 backup), PR #16, issue #4 comments 5016461138 (in-progress) / 5016525920 (handover).

### 2026-07-20 - SPEC-001 / #4 (PR #16, loop 2 — scope-audit defense)

- Context: Reviewer flagged scope drift claiming PR #16 re-implemented T5 `.github-project.json` additive-merge/idempotency code "around file lines 510-735 and call site 1574-1582". User instruction: compare against current `origin/master` (now includes merged T5), rebase if needed, remove any duplicated T5 implementation from the diff.
- Implementation Lesson: When a reviewer's scope finding cites FILE line numbers (not diff line numbers), the finding is likely a file-content-vs-diff misread. The right first move is to verify the 3-dot diff `git diff origin/master...HEAD` against the actual PR base. In this case `git merge-base(origin/master, HEAD)` was `2fc81fc` (= master tip with T5 merged), so GitHub's PR diff already excluded all T5 code; the T5 `ensure_github_project_config` function in the file was from master commit `2fc81fc`, not added by T3.
- Implementation Lesson (process): Always verify PR `base_sha` via `gh api repos/.../pulls/N --jq '{base_sha: .base.sha, mergeable, mergeable_state: .mergeableState}'` before assuming a rebase is needed. If `base_sha` equals the current master tip and `mergeable_state` reports MERGEABLE, the branch is already correctly based and a rebase would add no value. Rebasing a correctly-based branch only rewrites commit SHAs and can confuse reviewers.
- Implementation Lesson (process): When the scope audit confirms the diff is already correctly scoped (no duplicate code to remove), do NOT fabricate changes just to have something to commit. The correct action is a "no code changes needed" outcome documented with concrete evidence in the re-review handover: (a) branch lineage showing commits beyond master, (b) list of functions actually added by the PR, (c) grep evidence that the alleged duplicate only appears in master, (d) classification of test-file touches as unavoidable T3 consequences (version bump, dormant-test activation, pty stdin). Fabricating churn would itself be a KISS / scope-discipline violation.
- Implementation Lesson (process): Before declaring "no rebase needed", explicitly verify with three commands: `git merge-base origin/master HEAD` (should equal `git rev-parse origin/master`), `git log origin/master..HEAD --oneline` (the actual diff-introducing commits), and `git diff origin/master...HEAD --stat` (matches `gh api pulls/N/files`). All three agreed here: 1 commit (`01654b7`), 4 files, +1454/-29 — matching GitHub's view.
- Implementation Lesson: When a PR shares infrastructure with a sibling task that merged first (here T5's `.github-project.json` test file), some test-file edits are unavoidable T3 consequences, not scope drift: (a) version expectation `0.2.0 → 0.3.0` because T3 bumped `INIT_PROJECT_VERSION` per ARCH-003 / DM-1.3; (b) activation of a dormant boundary test (T5's `AC-T5-006`) that was always designed to activate when T3 shipped AGENTS.md generation; (c) pty stdin injection in T2's test helper because T3 added interactive prompts. These are cross-cutting consequences, not parallel/independent reimplementation of T5 behavior.
- Files/Modules: No code changes this loop (diff already correctly scoped). Verification commands rerun on the existing commit `01654b7`.
- Verification: `bash -n init_project_docs.sh` PASS; `node tests/test_agents_md_gen.js` 33/33; `node tests/test_inspect_repo.js` 187/187; `node tests/test_skills_copy.js` 53/53; `node tests/test_github_project_config.js` 48/48; `node tests/test_cli_flags.js` 81/81. Total 402/402. Smoke: noninteractive end-to-end on repo-node-npm fixture generates correct AGENTS.md (timestamp `v0.3.0`, DM-2.2 H2 sections, detected stack + commands traceable to inspection).
- Avoid: Do NOT rebase a branch that is already on the master tip "to be safe" — it rewrites SHAs and creates review noise without any scope benefit. Verify the merge-base first.
- Avoid: Do NOT add cosmetic changes (extra comments, refactors) just to manufacture a commit when a reviewer finding is based on a misread. Address the finding with evidence in the PR handover; let the reviewer re-review with the diff-scope clarification.
- Process Note: Issue #4 project item ID confirmed stable: `PVTI_lADOAGcCyM4Bdw3LzgzSC1I` (workflowState field `PVTSSF_lADOAGcCyM4Bdw3LzhYQD90`, in-review option `3cbc15c8`). Issue moved In Progress → In Review via direct GraphQL mutation (helper script still shadowed by stale placeholder values).
- Related Docs: `docs/spec/SPEC-001-init-project-tailored-repo-bootstrap.md`, `docs/arch/ARCH-003-project-local-initialization-artifacts.md` (Artifact 1 vs Artifact 2 separation), PR #16 comment 5016585883 (re-review handover with diff-scope analysis), issue #4 comment 5016586786 (loop-2 summary).

### 2026-07-20 - SPEC-001 / #4 (PR #16, re-review)

- Context: Builder re-review confirmed the earlier T5 scope objection was a base-content/diff misread; PR #16 diff stayed T3-only.
- Implementation Lesson: Before respinning code after a review finding on a PR that depends on merged base work, verify `git merge-base origin/master <head>` and `git diff origin/master...<head>` against the current base. If the alleged code already lives in master, do not add churn or rebase just to satisfy the finding; respond with diff evidence.
- Files/Modules: PR #16, `init_project_docs.sh` (no code changes in this loop)
- Verification: `bash -n .opencode/skills/project-initialization/scripts/init_project_docs.sh`; `node tests/test_agents_md_gen.js`; `node tests/test_cli_flags.js`; `node tests/test_github_project_config.js` in detached worktree
- Avoid: Rewriting a correctly-based branch when the only issue is diff-scope interpretation.
- Related Docs: `docs/spec/SPEC-001-init-project-tailored-repo-bootstrap.md`, `docs/arch/ARCH-003-project-local-initialization-artifacts.md`, PR #16 comment 5016585883, issue #4 comment 5016586786.

### 2026-07-20 - SPEC-001 / #4 (PR #16, merged to Done)

- Context: PR #16 merged by tech-lead into master at commit `6e9741f`. All acceptance criteria passed (AC-T3-001..008). 402/402 tests green. Reviewer approved after loop-2 diff-scope clarification resolved the T5-scope misread.
- Implementation Lesson: The spec's primary business value (repository-tailored AGENTS.md) is now live. All six FR-3.2 prompts derive from inspection evidence or are explicitly operator-provided. No fabricated facts. No placeholder text.
- Implementation Lesson: The cross-suite test adaptations in `test_github_project_config.js` (version bump, dormant-test activation) and `test_cli_flags.js` (pty stdin injection) were unavoidable T3 consequences, not scope drift. When shipping a spec-critical capability that changes a version constant and adds interactive prompts to a previously-prompt-free code path, proactively communicate these cross-cutting effects in the PR description to preempt scope-discipline findings.
- Files/Modules: `.opencode/skills/project-initialization/scripts/init_project_docs.sh` (+757 lines), `tests/test_agents_md_gen.js` (new, 33 tests), `tests/test_cli_flags.js` (+13/-1), `tests/test_github_project_config.js` (+37/-37).
- Verification: `node tests/test_agents_md_gen.js` 33/33; all regression suites 369/369. Total: 402/402.
- Avoid: Deferred — `--dry-run` file suppression (#7), idempotency overhaul (#7), `agent.md` content migration (FR-6.1 out of scope).
- Related Docs: `docs/spec/SPEC-001-init-project-tailored-repo-bootstrap.md`, `docs/arch/ARCH-003-project-local-initialization-artifacts.md`, PR #16, issue #4, commit `6e9741f`

### 2026-07-20 - SPEC-001 / #7 (PR #17, loop 1)

- Context: SPEC-001-T6 — implement OBS-1 structured prefixes, OBS-2 true no-write dry-run, ERR-1 pre-flight validation, ERR-2.1 atomic writes, ERR-2.3 trap cleanup, TR-2.1/2.2 idempotency, and fix the pre-existing `cp -Rn` non-portable warning. Single implementation file modified per guardrail (`init_project_docs.sh`); two new test files added.
- Implementation Lesson: The cleanest dry-run architecture is a small set of `emit_*` helpers (`emit_write` / `emit_merge` / `emit_warning` / `emit_skip`) that swap `[writing]` → `[would-write]` based on `opt_dry_run` and bump counter globals. Defined EARLY (right after `set -euo pipefail`), resolved at call time so they safely reference `opt_dry_run` which is parsed later in Phase 2. Every operator-facing line then flows through exactly one helper, so the final `[summary]` counts are always consistent with what was emitted.
- Implementation Lesson: For atomic-write + trap-cleanup across mixed bash/node code, split COMPUTE (node, no writes) from WRITE (bash, owns the atomic temp+rename + trap registration). Node helpers print a decision verb (`NO_CHANGE` / `WRITE` / `CREATE` / `UPDATE` / `MALFORMED`) + optional content; bash captures via `decision="$(node ...)"`, dispatches on the verb, and calls a shared `write_file_atomic` that registers the temp path with the EXIT trap. This is the only way bash can own trap cleanup for temps created inside node-written logic; otherwise an interrupt between `fs.writeFileSync` and `fs.renameSync` leaks a temp the bash trap doesn't know about.
- Implementation Lesson: `local var="$(cmd)"` is a bash gotcha — `local` masks the command's exit status (always returns 0). For capturing node helper status, declare `local var` on its own line, then `var="$(cmd)"` as a separate statement, THEN read `$?`. Under `set -euo pipefail` the separate-statement form correctly propagates non-zero exits.
- Implementation Lesson: TR-2.2 (`--force` N times produces identical AGENTS.md) is impossible to satisfy with a real timestamp in the generation header — two runs at different wall-clock times will always differ. The right primitive is **content-level idempotency**: strip the volatile generation-comment line (line 1) from BOTH existing and fresh content before comparing; if structurally equal, downgrade the write to a skip (no backup, no rewrite, preserve original timestamp). This makes `diff AGENTS.md /tmp/first.md` after two `--force` runs empty. A genuine content change still backs up + rewrites per ERR-3.2. The same pattern (strip-and-compare structural state, write only on diff) already powered T5's `.github-project.json` idempotency — reused here for AGENTS.md.
- Implementation Lesson: For ERR-1 pre-flight on a target git repo, check `.git` existence (`-d` or `-e`), NOT `git rev-parse`. The init test fixtures create an empty `.git/` directory marker; `git rev-parse` would reject those valid fixtures and break 402 existing tests. ERR-1.1 literally says "has `.git/`" — the marker check is both spec-faithful and test-compatible.
- Implementation Lesson: `cp -Rn` emits a non-portable warning on GNU coreutils ≥9 (`cp: warning: behavior of -n is non-portable and may change in future; use --update=none instead`). The portable replacement is a find-based recursive copy that skips existing targets: `while IFS= read -r -d '' src_file; do [[ -e "$tgt/$rel" ]] && continue; mkdir -p "$(dirname "$tgt/$rel")"; cp -p "$src_file" "$tgt/$rel"; done < <(find "$src" -type f -print0)`. Same merge semantics as `cp -Rn`, portable to BSD/GNU/Git-for-Windows, no warning. Wrap as `copy_tree_no_clobber`.
- Implementation Lesson: ERR-2.3 single-TEMP_DIR-with-trap pattern: `TEMP_DIR="$(mktemp -d)"` + `trap cleanup_temp EXIT` + `trap 'cleanup_temp; exit 130' INT` + `trap 'cleanup_temp; exit 143' TERM`. Atomic-rename temps that MUST live in the target dir (same-filesystem rename) get registered in a `CLEANUP_FILES=()` array that `cleanup_temp` also iterates. The guardrail "subsequent traps must append, not replace" means: set the EXIT trap ONCE early; later signal traps call the same `cleanup_temp` function rather than replacing the EXIT handler.
- Implementation Lesson: `INIT_PROJECT_VERSION` should only bump when the `initMeta.version` schema/capability contract changes (per ARCH-003 / DM-1.3). T6 changed behavior (observability/dry-run/idempotency) but NOT the DM-1/DM-2 schema, so the version stayed at `0.3.0`. This kept the existing T3 version-assertion test green without modification and is the defensible read of "version = artifact schema stamp, not implementation version".
- Implementation Lesson (test design): For dry-run no-write assertions, compare `fs.readdirSync(tmp)` BEFORE and AFTER the dry-run (deep-strict-equal), not just "AGENTS.md doesn't exist". A dry-run that quietly created an intermediate `.opencode/` directory but no files inside would pass a file-existence check but fail a directory-listing check. The strongest assertion: after dry-run, `tmp` contains ONLY the pre-run `.git` marker.
- Implementation Lesson (test design): For idempotency assertions, use a content hash (`crypto.createHash('sha256')`) of the file BEFORE and AFTER a rerun, not `diff` against a saved snapshot. Hash comparison is O(1) in test code and gives a clean assertion message; `diff` requires a temp file and shell-out. Both work; hash is cleaner in Node.
- Files/Modules: `.opencode/skills/project-initialization/scripts/init_project_docs.sh` (counters + trap + emit_* helpers + run_preflight + write_file_atomic + copy_tree_no_clobber + strip_agents_md_header; rewrote ensure_opencode_config + ensure_github_project_config to compute-only-node + atomic-bash-write; dry-run guards on every write path; AGENTS.md content-level idempotency; final [summary] line; +535/-121 lines), `tests/test_dryrun.js` (new, 15 tests: AC-T6-002/003/004, OBS-1/2, ERR-1), `tests/test_idempotency.js` (new, 15 tests: AC-T6-006/007/008, TR-2.2, ERR-2.3).
- Verification: `node tests/test_dryrun.js` → 15/15; `node tests/test_idempotency.js` → 15/15; all existing suites unchanged (33+81+48+187+53 = 402). Total: 432/432. Issue-body verification: `diff AGENTS.md /tmp/agents-first.md` after two `--force` runs → empty; dry-run → only `.git` remains in target; non-git dir → exit 1 + `[error]`; `--force` on changed AGENTS.md → `.bak` created, second identical `--force` → no new `.bak`.
- Avoid: Do NOT bump `INIT_PROJECT_VERSION` for a behavior-only change — the version tracks the DM-1/DM-2 schema stamp (ARCH-003 / DM-1.3), not the implementation changelog. Bumping it breaks existing version-assertion tests for no schema reason.
- Avoid: Do NOT use `fs.writeFileSync` directly in node helpers that bash invokes — it bypasses the bash trap and leaks temps on interrupt. Always route through the compute-then-bash-write split so `write_file_atomic` + `register_cleanup` own the temp lifecycle.
- Avoid: Do NOT check `git rev-parse` for ERR-1.1 "is a git repo" — it rejects the empty-`.git`-marker test fixtures. Use `.git` existence.
- Avoid: Do NOT accumulate `.bak` files on every `--force` rerun — that violates the "no artifact count increases" spirit of TR-2.1. Use content-level idempotency so an identical-content `--force` rerun skips the backup + write entirely; only a genuine content change creates a `.bak`.
- Avoid: Do NOT touch #8 (e2e/migration), #9 (AGENTS.md validation script), #10 (skill/docs update), or #11 (self-init) when implementing T6 — they have explicit dependencies on T6 and ship separately.
- Process Note: Issue #7 project item ID is `PVTI_lADOAGcCyM4Bdw3LzgzSC2U` (workflowState field `PVTSSF_lADOAGcCyM4Bdw3LzhYQD90`, in-review option `3cbc15c8`). The `gh_project_helper.sh set-status` helper still shadows the real project ID with the placeholder `PVT_kwDOEXAMPLE` from `.github-project.env` (helper line ~340 overrides resolved ID with `${PROJECT_ID:-}` from env). Direct `gh project item-edit --id --project-id --field-id --single-select-option-id` works. Flagged as a future tooling task.
- Related Docs: `docs/spec/SPEC-001-init-project-tailored-repo-bootstrap.md` (FR-9, OBS-1/2/3, ERR-1/2/3/4, TR-2, AC-SPEC-005/009/010), `docs/arch/ARCH-003-project-local-initialization-artifacts.md` (guardrails 6 + 7: no files outside target except temp dir; verify dry-run writes zero files), PR #17, issue #7 comments 5022572130 (tech-lead delegation) / 5023078080 (builder handover).

### 2026-07-21 - SPEC-001 / #9 (AGENTS.md validation script)

- Context: Implemented pure-bash structural validator for AGENTS.md (ARCH-003 DM-2 contract). Merged at `b2ff793`.
- Implementation Lesson: The init-project generator emits directory entries in `## Local Configuration Files` (e.g., `.opencode/skills/foo/`). Path existence checks must use `-e`, not `-f`, or valid generator output will be falsely rejected. Trailing slashes should be stripped before resolution.
- Implementation Lesson: Repo-root resolution for AGENTS.md file-existence checks: resolve as the parent directory of the AGENTS.md argument, not CWD. This lets the validator run from anywhere and still resolve LCF paths correctly.
- Files/Modules: `scripts/validate-agents-md.sh`, `tests/test_validate_agents_md.js`
- Verification: `bash -n scripts/validate-agents-md.sh`; `node tests/test_validate_agents_md.js` (34/34); full regression `node tests/test_*.js` (436/436)
- Avoid: Using `-f` for LCF path checks — use `-e`. Don't add content-quality grading — structural-only is the intended scope.
- Related Docs: SPEC-001 TEST-4.2, ARCH-003 DM-2, issue #9, PR #18

### 2026-08-01 - SPEC-001 / #11 (PR #20, self-init, loop 1)

- Context: SPEC-001-T10 — run the upgraded `init-project` (v0.3.0) against the ant-teams SOURCE repo itself (the reference implementation validating the full init pipeline on a real repo). Final task of SPEC-001.
- Implementation Lesson: When self-initializing from a dedicated issue worktree, the default worktree-root derives from `basename "$project_dir"` (init line ~1908), NOT from `--name`. A worktree at `~/Projects/worktree/ant-teams/issue-11` yields a wrong default `~/Projects/worktree/issue-11`. MUST pass `--worktree-root ~/Projects/worktree/ant-teams` explicitly so the mkdir target, the opencode.json `external_directory` entry, and the `.github-project.json` `worktreeRoot` all agree on the canonical value. The `.github-project.json` `worktreeRoot` is preserved verbatim regardless (FR-6.3 additive-only), but the side-effect writes use the computed default.
- Implementation Lesson: A `.github-project.json` that was manually populated with the FULL DM-1 schema during planning (worktreeRoot + identity + boundaries + initMeta all present) is an idempotent no-op for `ensure_github_project_config` — the strip-and-compare (generatedAt removed from both sides) finds structural equality and emits "No changes needed" without refreshing `initMeta.generatedAt` or bumping `initMeta.version`. So a planning file stamped `0.1.0` stays `0.1.0` even though `INIT_PROJECT_VERSION=0.3.0`; this is correct additive behavior, not a regression.
- Implementation Lesson: For self-init where source repo == target repo, `copy_tree_no_clobber "$repo_root/docs"` copies 0 files (every source file already exists in target) and the docs scaffold only `mkdir -p`s empty directories — git tracks neither, so the docs/skills steps produce zero git-observable changes. The `[writing] docs/` / `[writing] .opencode/skills/` lines are reported but are effective no-ops.
- Implementation Lesson: AC literalism vs FR-5.3. AC-T10-002 ("stack shows Node.js/npm") is UNMETTABLE for this repo because there is no root `package.json` (only `.opencode/package.json` for the opencode plugin). Inspection truthfully reports `not detected`; FR-5.3 forbids fabricating a Stack section; DM-2.3 mandates omitting empty sections; CLI-2 defines NO `--stack` operator-input flag. The honest AGENTS.md has no Stack section. The Commands section IS populable via `--commands @file` (FR-4.1) with verified-accurate test commands. Do NOT hand-fabricate a Stack section to satisfy the AC literal text — flag it to reviewer/tech-lead instead.
- Implementation Lesson: AC-T10-004 ("no extra skills") is a TARGET-repo contract, not a SOURCE-repo one. The ant-teams repo OWNS the full skill set that init copies FROM; init is additive-only (FR-7) and never deletes source skills. Init correctly emitted `[warning] Excluded skill already present` for the 4 non-required skills it recognized (skill-creator, webapp-testing, doc-coauthoring, frontend-design) without removing them. This is correct behavior.
- Implementation Lesson: `ensure_opencode_config` writes opencode.json via node `JSON.stringify(parsed, null, 2)` which does NOT add a trailing newline. A previously-newline-terminated opencode.json loses its trailing newline on first init write. Commit the file as-produced (do NOT hand-re-add the newline) — otherwise a rerun removes it again and idempotency (TR-2.1) breaks. The trailing-newline delta is init's genuine behavior, visible in every fresh init.
- Implementation Lesson (scoped commit discipline): The self-init produced exactly 4 git-observable files — `AGENTS.md` (new), `.github-project.json` (new = the seeded planning file), `.opencode/opencode.json` (modified = external_directory added), `.opencode/.gitignore` (new = node_modules). Stage ONLY these 4 explicitly (`git add <files>`) — never `git add -A`/`git add .`, because the main workspace and worktree can carry untracked local-only artifacts (logs, memory, communication, board) that must not be committed.
- Files/Modules: `AGENTS.md` (new), `.github-project.json` (committed here), `.opencode/opencode.json` (+external_directory), `.opencode/.gitignore` (new). No init code changes (this task runs the existing pipeline, does not modify it).
- Verification: `bash scripts/validate-agents-md.sh AGENTS.md` → 11/11; full suite `for f in tests/test_*.js; do node "$f"; done` → 8 suites / 466 assertions / 0 fail; idempotent rerun → exit 0 / 0 created / 1 skipped; `.github-project.json` byte-identical to pre-init planning file.
- Avoid: Do NOT pass `--force` to regenerate AGENTS.md when a cleaner path exists (remove the file first) — `--force` creates `AGENTS.md.bak.<ts>` noise via ERR-3.2. Removing AGENTS.md then re-running init regenerates it fresh with no backup artifact.
- Avoid: Do NOT commit `.opencode/opencode.log`, `docs/memory/`, `docs/proj-management/communication/`, or `docs/proj-management/board.md` as part of a self-init commit — those are local runtime/memory artifacts, not init output. The user explicitly excluded local memory and logs.
- Avoid: Do NOT include issue #10 docs work or the `~/.agents` follow-on idea in this commit — explicitly out of scope per founder instruction.
- Process Note: Issue #11 project item ID is `PVTI_lADOAGcCyM4Bdw3LzgzSC4U` (workflowState field `PVTSSF_lADOAGcCyM4Bdw3LzhYQD90`, options: in-progress=`430925ac`, in-review=`3cbc15c8`; legacy Status field `PVTSSF_lADOAGcCyM4Bdw3LzhYQDlE` has only todo/in-progress/done — no in-review, so leave it at in-progress when moving to In Review). Branch `task/SPEC-001-self-init` (issue guardrail prescribed this name, not the `feat/spec-001-tN-*` convention — self-init is a chore, not a feat). PR #20.
- Related Docs: SPEC-001 (FR-5.3, FR-6.3, DM-2.3, CLI-2, TR-2.1), ARCH-003 (Artifact 1/4, guarantee 2 strict additivity), issue #11 (activation comment 5144049394), PR #20.

### 2026-08-01 - SPEC-001 / #11 (merged to Done)

- Context: Self-initialized ant-teams with upgraded init-project v0.3.0. Tech-lead merged PR #20 after final spec-alignment check.
- Implementation Lesson: Self-init of the source repo (the repo that owns init-project itself) is a special case — `copy_tree_no_clobber` for docs/skills is a no-op (source == target), and init correctly skips skills that already exist with `[warning] Excluded skill already present`. The "no extra skills" AC-T10-004 applies to TARGET repos; source-repo self-init retains its canonical skills per FR-7.2/7.3.
- Implementation Lesson: When the repo has no root `package.json` and the inspection engine reports `not detected`, AGENTS.md correctly omits the Stack section (FR-5.3 + DM-2.3). Do not fabricate a stack claim to satisfy a checklist — truthful output is the spec priority.
- Implementation Lesson: `--worktree-root` must be passed explicitly for self-init because the default derives from worktree directory basename (e.g., `issue-11`), which won't match the canonical `ant-teams` worktree root.
- Files/Modules: AGENTS.md (new, 4 sections), .github-project.json (new, DM-1 schema), .opencode/opencode.json (+5/-2: external_directory), .opencode/.gitignore (new: node_modules)
- Verification: `bash scripts/validate-agents-md.sh AGENTS.md` → 11/11, `for f in tests/test_*.js; do node "$f"; done` → 466/466
- Avoid: Fabricating stack claims for repos without root package.json. Do not delete source-repo skills during self-init.
- Related Docs: PR #20, issue #11, SPEC-001 FR-5.3/FR-7.2/7.3, ARCH-003

### 2026-08-01 - SPEC-001 / #10

- No new durable memory.

### 2026-08-01 - SPEC-001 / #10 (PR #21 — merged to Done, milestone closed)

- Context: PR #21 merged as `1ecd329`. Review loop 1 found README.md scope creep (unauthorized per issue #10 guardrails). Tech-lead confirmed — reverted README.md to master via `git checkout master -- README.md`. Loop 2 approved without blockers. Final spec-alignment check passed. Issue #10 Done, milestone #1 closed with all 10 issues complete.
- Implementation Lesson (scope enforcement): Even truthful, accurate documentation updates that expand beyond the enumerated file-modification contract are scope creep. "Folder / package / namespace rules" in the issue body IS the binding contract. If README.md is not listed, do not touch it — this applies symmetrically regardless of how correct the change would be.
- Implementation Lesson (PR review vs branch git state): When a reviewer inspects the repo working tree instead of the PR three-dot diff, they may misclassify base-branch files as "PR changes." The reviewer's scope finding on #10 was correct (README was in the PR), but the general lesson is: always compare against the PR base, not the local working tree.
- Implementation Lesson (untracked canonical docs): SPEC-001 and ARCH-003 were live working-tree files referenced by every implementation issue but never `git add`ed. Committing them alongside DOCUMENT_INDEX entries that reference them (when the issue explicitly requires those entries) is the smallest reconciliation. This is NOT self-initialization (which runs the init tool) — it's documentation hygiene.
- Files/Modules: PR net diff after revert: `SKILL.md` (80 +/-), `agent-md-template.md` (+2 deprecation comment), `DOCUMENT_INDEX.md` (+8 rows), `ARCH-003` (new, 283 lines), `SPEC-001` (new, 856 lines). README.md reverted (0 net change).
- Verification: Regression suites all green (466 unit + 10 e2e + 11 validator + bash syntax). All DOCUMENT_INDEX Path entries resolve on disk.
- Avoid: Do not bundle README or other "related" documentation into a task whose scope contract doesn't list them. Do not modify ARCH-003 guardrails/schema in a docs-only issue. Do not delete deprecated assets (agent-md-template.md) — mark and preserve per ARCH-003 backward-compat contract.
- Related Docs: PR #21, issue #10, SPEC-001, ARCH-003, milestone #1, commit `1ecd329`

### 2026-08-02 - SPEC-002 / #22 (merged to Done)

- Context: PR #26 merged by tech-lead at commit `4c5b2c5` into master. Issue #22 moved to Done. No new implementation lessons — merge was performed by tech-lead after 4 review loops completed.
- Implementation Lesson: None — tech-lead merge; no builder code was written.
- Files/Modules: N/A
- Verification: N/A
- Avoid: N/A
- Related Docs: PR #26, issue #22, commit `4c5b2c5`

### 2026-08-03 - SPEC-002 / #25 (PR #29 — In Review, unmerged)

- Context: Final SPEC-002 deliverable (T4 docs). Documentation-only; no implementation code touched. PR #29 on `feat/spec-002-t4-docs-update`, base `master` (`85591a7`). Worktree `/home/chrissim/Projects/worktree/ant-teams/issue-25`.
- Implementation Lesson (recurring untracked canonical docs): Same pattern as SPEC-001/#10 — ARCH-004 (335 lines) and SPEC-002 (922 lines) were live working-tree files referenced by the milestone, every issue, and ARCH-004 cross-refs, but were NEVER committed by T1/T2/T3 PRs (#26/#27/#28 only added scripts + tests). Committing them verbatim (verified accurate against `scripts/sync-managed-skills.sh` + `scripts/sync-company.sh`) alongside the DOCUMENT_INDEX entries that reference them is the documentation-hygiene fix and is in-scope for the docs issue (out-of-scope is *rewriting* them, not committing them).
- Implementation Lesson (docs-only verification recipe that works for this repo): (1) `bash scripts/validate-agents-md.sh AGENTS.md` (DM-2 contract, 11 checks); (2) `bash tests/run_sync_tests.sh` (managed-sync regression, 29 scenarios); (3) `for f in tests/test_*.js; do node "$f"; done` (8 JS suites); (4) DOCUMENT_INDEX Path-column link check via `awk -F'|' 'NR>2 {...$7...}'`; (5) cross-check any enumerated name lists in new docs against `git ls-tree -d origin/master .opencode/skills/` and `.opencode/commands/` using `diff` on sorted output.
- Implementation Lesson (exact command-derived skill names): The 8 commands transform to skills using the exact filename (no `cmd-` prefix): `deliver`, `do-tasks`, `fix-bug`, `migrate`, `new-spec`, `plan-sprint`, `sprint-clean`, `sync-spec`. The 26 source skills are the directories under `.opencode/skills/`. Total managed entries = 34. These numbers are baked into AC-2.1 / TR-3.1 and must match any doc that enumerates them.
- Implementation Lesson (runbook placement): No existing `docs/runbooks/` dir or RB-* ID family in this repo. Created `docs/runbooks/RB-001-managed-skill-sync.md` as the operator-facing companion to the canonical SPEC-002/ARCH-004, indexed as a new `runbook` type row in DOCUMENT_INDEX. This keeps README concise (pointer + 6-bullet subsection) while the runbook holds exact names + validation commands + rollback.
- Files/Modules: `docs/arch/ARCH-004-...md` (new), `docs/spec/SPEC-002-...md` (new), `docs/runbooks/RB-001-managed-skill-sync.md` (new), `docs/DOCUMENT_INDEX.md` (+4 rows: ARCH-004, SPEC-002, REF-007, RB-001), `README.md` (Install Model bullet + Managed Skill Mirror subsection). AGENTS.md intentionally untouched (init-project-generated, not stale).
- Verification: validate-agents-md 11/11; run_sync_tests 29/29; JS suite 8/8; bash -n OK; DOCUMENT_INDEX + README link check all resolve; runbook name lists exact match (26+8).
- Avoid: Do not rewrite ARCH-004/SPEC-002 content in the docs issue (out-of-scope) — commit verified-as-is. Do not add ARCH-003/SPEC-001 to DOCUMENT_INDEX (already indexed on master since SPEC-001-T9 `af5e582`). Do not touch AGENTS.md test-command list (generated; out of scope). Do not merge — tech-lead owns final check + merge.
- Related Docs: PR #29, issue #25, SPEC-002, ARCH-004, RB-001, milestone #2, activation comment (2026-08-03T12:16:27Z)
### 2026-08-03 - SPEC-002 / #25 (PR #29, merged to Done)

- Context: SPEC-002-T4 — documentation-only deliverable, the final SPEC-002 issue. No implementation code changed. Added 3 new docs (ARCH-004, SPEC-002, RB-001) that were referenced by T1-T3 but never committed, updated DOCUMENT_INDEX (+4 rows: ARCH-004, SPEC-002, REF-007, RB-001), and added a concise Managed Skill Mirror subsection to README.
- Implementation Lesson (docs reconciliation pattern): ARCH-004 and SPEC-002 were canonical reference docs drafted during SPEC-002 shaping and read by every builder during T1-T3, but lived as untracked working-tree files throughout. T4 was the correct sequencing — commit them LAST as documentation of what was built, not as prerequisite planning. DOCUMENT_INDEX cross-references only resolve correctly after the docs are committed.
- Implementation Lesson (runbook for operators, not spec rehash): RB-001 intentionally duplicates NO architecture or spec rationale. It's a concise operator reference: listing (26 skills + 8 commands with exact names), flag semantics, modification/force/collision behavior, rollback steps, scope cuts, and validation commands. This separation prevents operators from needing to read the 922-line spec to understand day-to-day sync behavior.
- Files/Modules: `docs/arch/ARCH-004-managed-skill-sync-architecture.md` (new, 335 lines), `docs/spec/SPEC-002-managed-global-skill-sync-and-command-derived-skills.md` (new, 922 lines), `docs/runbooks/RB-001-managed-skill-sync.md` (new, 213 lines), `docs/DOCUMENT_INDEX.md` (+4 rows), `README.md` (+12/-1 Managed Skill Mirror subsection).
- Verification: `validate-agents-md.sh` 11/11; `run_sync_tests.sh` 29/29; JS suites 8/8; shell syntax OK; DOCUMENT_INDEX paths all resolve; README doc-reference links all resolve; runbook names vs repo exact match.
- Avoid: Do NOT rewrite ARCH-004 or SPEC-002 (committed as-is — out of scope). Do NOT modify AGENTS.md (init-project-generated, not stale). Do NOT add implementation, test, or configuration changes (docs only).
- Related Docs: SPEC-002, ARCH-004, RB-001, DOCUMENT_INDEX, PR #29, issue #25.

### 2026-08-22 - founder-directed / project metadata runtime (no issue — builds on uncommitted workflow revision)

- Context: Implement the `.github-project.env` runtime (generated `ANT_TEAM_*` exports) layered ON TOP of the founder's uncommitted workflow revision in the main checkout (Workflow State canonicalization, ANT_TEAM_SCRIPTS routing, INV-1..9 locked by untracked `tests/test_workflow_invariants.js`). No GitHub issue exists; work done directly in the main workspace, uncommitted, preserving the founder's staged `migrate.md` deletion and all unstaged edits.
- Implementation Lesson (canonical env contract): `scripts/generate-github-project-env.sh` reads `.github-project.json` → writes `.github-project.env` with `ANT_TEAM_GITHUB_{OWNER,OWNER_TYPE,REPO,PROJECT_NUMBER,PROJECT_ID,STATUS_FIELD_ID,WORKFLOW_STATE_FIELD_ID}`, `ANT_TEAM_GITHUB_{STATUS,WORKFLOW_STATE}_OPTION_<KEY>_ID` (key = uppercase, non-alnum → `_`), `ANT_TEAM_WORKTREE_ROOT`, `ANT_TEAM_DOCS_{VAULT_PATH,PROJECT_NAME,PROJECT_PATH_TEMPLATE,PROJECT_PATH,REPOSITORY}` (`PROJECT_PATH` = template with `<project-name>` resolved). `gh_project_helper.sh` resolves ANT_TEAM_* FIRST, then JSON, then legacy unprefixed names. Values single-quoted via jq `@sh` (apostrophe-safe round-trip verified).
- Implementation Lesson (jq portability gotcha): this machine's `jq-1.7` does NOT define `ascii_uppercase` (gojq-style `ascii_upcase` exists instead). Do NOT use either builtin name — inline `explode|map(if .>=97 and .<=122 then .-32 else . end)|implode` is portable across builds.
- Implementation Lesson (idempotency comparison): `"$(cat file)" == "$content"` is WRONG for byte-exact checks — command substitution strips trailing newlines. Use `printf '%s' "$content" | cmp -s - file`. This bit the generator's `[unchanged]` path (reruns reported `[created]` and rewrote).
- Implementation Lesson (init integration): `generate_project_runtime_env` resolves the generator at `$repo_root/scripts/` (source checkout) then `$ANT_TEAM_SCRIPTS/`; missing generator or jq = emit_warning + continue (init's TR-1.1 contract is bash+node+coreutils — do NOT hard-fail). In dry-run with no existing JSON, report `[would-write] .github-project.env` without invoking the generator (avoids spurious exit-3 warnings). `documentation.projectName` defaults to repo basename (NOT `--name`), never overwrites a founder value; node helper takes `repo_name` as a NEW 8th positional arg after `version`.
- Implementation Lesson (fake-gh behavioral tests): to prove env-primary vs JSON config in `gh_project_helper.sh`, generate the env, THEN edit the JSON to diverge, and run with a fake `gh` script prepended to PATH that appends `"$@"` to a log — asserts the owner from the env, not the JSON. Fixture repos use a `.git` marker dir (not real repos) so the helper's `git rev-parse --show-toplevel || pwd` falls back to cwd — invoke with `cwd: fixtureDir`.
- Process Note (pre-existing smoke failure — NOT from this task): `tests/e2e/test_smoke_sync_managed_skills.sh` F1 expects 34 NOOPs but gets 33 because the founder's STAGED deletion of `.opencode/commands/migrate.md` (INV-7 /migrate retirement) drops one command-derived managed entry. Out of scope here; whoever owns that revision should update the expectation.
- Files/Modules: `scripts/generate-github-project-env.sh` (new), `tests/test_project_env_runtime.js` (new, 18 checks ENV-1..16), `init_project_docs.sh` (projectName + env step + AGENTS.md generator tweaks), `gh_project_helper.sh` (env-primary resolution), `create/cleanup_task_worktree.sh` (env-first worktree root), SKILL.md ×3 + `command-patterns.md` + `new-spec.md` + `AGENTS.md` + `opencode.json` prompts ×5 + `.github-project.json/.env`.
- Verification: 10/10 node suites (509 assertions incl. workflow invariants 25/25); `run_e2e_tests.sh` 10/10; `validate-agents-md.sh` 13/13; `bash -n` all touched scripts clean; repo env regenerates `[unchanged]`.
- Avoid: Do NOT hand-edit `.github-project.env` (generated; regenerate after JSON edits). Do NOT add `ascii_uppercase`/`ascii_upcase` jq builtins to scripts. Do NOT make jq a hard init dependency. Do NOT use `$(cat)` equality for idempotency.

### 2026-08-22 - founder correction / project-env hard migration into init (no issue — same uncommitted revision)

- Context: Founder corrected the 2026-08-22 project-env implementation above: remove the standalone `scripts/generate-github-project-env.sh`, do not expose or use it as a separate command, and integrate JSON-to-`.github-project.env` generation directly into project initialization (hard migration) — after creating or migrating `.github-project.json`, init directly writes/overwrites the env from the FINAL on-disk JSON, replacing stale values. `documentation.projectName` default (git repo basename) and founder override stay intact. sync-company stays the prerequisite for project-init in the agent harness.
- Implementation Lesson (hard migration): `generate_project_runtime_env` in `init_project_docs.sh` now generates the env via an INLINE node helper (node is already a hard preflight requirement) — init no longer needs jq at all. Content is always rebuilt from the final JSON; byte-identical content is skipped (idempotent, stable mtime), any difference (stale/garbage env or edited JSON) rewrites via `write_file_atomic`. Helper failure is a hard `[error]` exit 1. Env header says "Generated by init-project … Regenerate by re-running project initialization" — no standalone command exists.
- Implementation Lesson (jq→node port): `@sh` parity = single-quote with `'` → `'\''`; option-key normalization = ASCII upcase then `[^A-Z0-9]` → `_`; `PROJECT_PATH` = first-occurrence `String.replace("<project-name>", projectName)` (jq `sub` parity). Byte-compat with the old jq output verified (exports identical; only the header changed).
- Files/Modules: `scripts/generate-github-project-env.sh` (DELETED), `init_project_docs.sh` (inline env generation), `tests/test_project_env_runtime.js` (rewritten: generation tested THROUGH init — jq-absent PATH proof, malformed-JSON no-env, hard-migration stale replacement, dry-run statuses; ENV-1..16), `AGENTS.md`, `project-initialization/SKILL.md`, `github-issues-projects-cli/SKILL.md` + `command-patterns.md` + `gh_project_helper.sh` (regeneration guidance → re-run init-project), `.github-project.env` (regenerated via the new init path), `tests/e2e/test_smoke_sync_managed_skills.sh` (F1 34→33, pre-existing stale expectation from the staged migrate.md retirement, flagged by the previous session).
- Verification: `node tests/test_project_env_runtime.js` 16/16; full node suite 10/10; `run_e2e_tests.sh` 10/10; `run_sync_tests.sh` 29/29; standalone sync smoke green after F1 fix; `bash -n` clean; `rg generate-github-project-env` → only intentional "was removed" notes + this memory.
- Avoid: Do NOT resurrect a standalone env generator command (founder-corrected architecture). Do NOT hand-edit `.github-project.env` — re-run init-project after JSON edits. Do NOT reintroduce a jq dependency into init env generation.
- Related Docs: builder-memory entry above (2026-08-22 project metadata runtime), `project-initialization/SKILL.md` step 3.
### 2026-08-22 - reviewer fix / helper env-primary evaluation order (no issue — same uncommitted revision)

- Context: Reviewer finding on the 2026-08-22 project-env runtime (entries above): `gh_project_helper.sh` preferred env *values* but *evaluated* `json_get` before the ANT_TEAM_* check in all six `resolve_*` functions, so a malformed `.github-project.json` spewed `jq: parse error` stderr on every sourced-env invocation (repro: exit 0 but 2 parse-error lines; one nesting level shallower the same failure aborts under `set -e`). Violated the helper's own documented contract "env is PRIMARY; JSON is read directly only for values the env does not provide".
- Implementation Lesson (preference vs evaluation order): "env wins" is not enough — a `value="$(json_get ...)"` computed before the env check still parses the JSON. Env-first must mean the JSON read lives only inside the else-branch reached when the env var is empty. Same shape applies to any future resolver: explicit arg -> ANT_TEAM_* -> JSON -> legacy unprefixed -> (remote discovery at call sites).
- Implementation Lesson (negative-test discipline): prove a new regression check fails on the buggy code — restore the bug, watch the check fail, re-apply the fix. My first scripted revert errored silently and the "pass" proved nothing; re-verified with direct edits (ENV-14b FAIL on the parse-error assertion, suite exit 1, then green after restore).
- Files/Modules: `gh_project_helper.sh` (6 resolve functions reordered + order comments), `tests/test_project_env_runtime.js` (ENV-14b: variant A malformed JSON + generated env; variant B deleted JSON/env file + exported-only env; header comment).
- Verification: `node tests/test_project_env_runtime.js` 18/18; all 10 node suites green; `validate-agents-md.sh` 13/13; `run_e2e_tests.sh` 10/10; manual repro now 0-byte stderr with env values used; JSON fallback (env absent) manually verified (`json-fallback-owner`).
- Avoid: Do not evaluate `json_get` before the ANT_TEAM_* check in any resolver. Keep ENV-14b's zero-stderr assertion — it is the "no JSON parse on the env path" proof. Do not harden `json_get` itself against malformed JSON (fallback behavior preserved as-is; out of scope).
- Related Docs: builder-memory entries 2026-08-22 "project metadata runtime" and "project-env hard migration into init"; helper usage notes (runtime config resolution).
