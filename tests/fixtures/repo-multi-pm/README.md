# Demo Multi Package Manager

Fixture repository with both `go.mod` and `package.json` at the repository
root. The inspection engine must flag this as an ambiguity rather than silently
selecting either stack.
