# Legacy agent.md

This file represents the legacy lowercase `agent.md` guidance file produced by
the older `setup_project_docs.sh` initializer. The upgraded initializer must
detect it but must never delete it.

Recommended docs root: `docs/`.
