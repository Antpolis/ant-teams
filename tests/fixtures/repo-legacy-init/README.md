# Legacy Init Demo

Fixture repository representing an older initialization produced by
`setup_project_docs.sh`. The upgraded initializer must preserve all pre-existing
fields when rerun on this repository.
